# Estado del Desarrollo — almacen.tienda

> **Actualizar este archivo cada vez que se resuelva un bug, se complete una feature o aparezca algo nuevo.**
> Fecha de última actualización: 2026-09-15 (Comisión/Gestor saldo negativo permitido + Devolución de Venta + Operaciones Múltiples + fixes de Productos)

---

## Rama activa

`feature/desarrollo-caliente`

---

## 🎯 Lista de tareas priorizada

> Backlog de trabajo consolidado (tests + frontend + bugs conocidos + limpieza). Actualizar el estado (`[ ]`→`[x]`) según se avance, y mover/quitar ítems cuando cambien de prioridad. No duplica el detalle — enlaza a la fuente cuando existe (memoria de progreso de tests, `pendiente-combobox-reemplazo.md`, etc.).

### Alta prioridad

_(Ninguna pendiente por el momento — el último ítem, insignias visuales de `Monedas/Create.tsx` y `Monedas/Show.tsx`, se cerró el 2026-09-14. Ver historial.)_

### Media prioridad
- [ ] **Devolución de Venta — falta la Fase 5 (Cierre de Caja)**: el resto de la feature (estado `devuelta`, botón/diálogo, badges, Rastreo de Operaciones, notificación Telegram) quedó completo e implementado 2026-09-15, ver historial. Falta agregar una sección separada **"Ventas Devueltas"** en el resumen del Cierre de Caja (backend `CierreCajaController.php` líneas ~587 y ~1552 + las 2 vistas `Cierres/Create.tsx`/`Cierres/Show.tsx`), distinta de "Ventas Anuladas" — decisión ya confirmada por el cliente (quiere las dos secciones separadas, no combinadas). Hasta que esto se agregue, una venta devuelta **no aparece contada en ningún resumen del Cierre**.
- [ ] **Ventas #32 y #33 de prueba siguen en la base de datos de desarrollo** — #32 (QA-TEST DEVOLUCION) usada para verificar el flujo de Devolución 2026-09-15; #33 (CONECTOR, cliente TEST PRUEBA) usada para verificar en navegador que aprobar deja la cuenta de comisión en negativo, mismo día. Ambas ya revertidas correctamente vía Devolución (no afectan ningún saldo real, confirmado en BD). Pendiente confirmar con el cliente si se borran o se dejan como registro histórico de las pruebas.
- [ ] **Cuentas → tarjetas de banco — decisión pausada por el cliente, sin resolver todavía:** qué dato ocupa el primer badge de pie de cada card (hoy usa `tipo_titular` — Externa/Personal — como marcador de posición; el cliente pidió dejarlo para después, no asumir que es la respuesta final). También pendiente: asignar banco a las ~79 cuentas tipo tarjeta reales que hoy quedan "sin banco asignado" (uno por uno, a pedido del cliente). **Efectivo ya no está pendiente** — cerrado 2026-09-09, insignias reales USD/CUP/EUR, ver historial. Ver historial 2026-09-08 para el detalle completo de la feature de bancos.
- [ ] **`Vendor/Show.tsx` — pasada de UX/UI (headers con degradado), 4 de 8 cards hechas**: ya tienen el header degradado (`docs/patron-card-header-degradado.md`): Distribución de la Venta (emerald), Productos Vendidos (indigo), Detalles de Pago (sky), Resumen Financiero (teal). Faltan 4 cards en `bg-card` plano: "Destinatario", "Comisión Punto de Venta", "Gestor - Comisión", y una cuarta sin confirmar (posible mensajero) — reverificado por grep 2026-09-08, siguen siendo solo 4 `CardTitle` con `text-white`/degradado en todo el archivo; confirmar números de línea antes de tocar, se mueven con cada edición. No arrancar ninguna sin que el cliente la nombre primero (disciplina de cola de a un ítem). Pausado además por el cliente, sin fecha: los sub-bloques internos de "Distribución de la Venta" (Resumen de cobro, Mensajería, Comisión Vendedor/Gestor) y el rediseño visual completo de "Detalles de Pago" (logos de banco/bandera/badges, necesita nueva migración en `cuentas`, esquema aún sin decidir). Detalle completo en memoria `project_vendor_show_ux_pass` y `project_detalles_pago_rediseno_visual`.
- [ ] **Tests — RBAC/permisos por rol** (vendedor limitado a sus almacenes/cuentas, `precio_compra_producto`/`costo_unitario` ocultos para vendedor). No revisado a fondo en la pasada del 2026-09-08.
- [ ] **Tests — Productos, precios por vendedor, monedas** (único `producto_id`+`almacen_id` en `producto_vendedors`, impacto de cambio de tasa en cuentas). No revisado a fondo en la pasada del 2026-09-08.
- [ ] **Combobox — pendiente real, reverificado 2026-09-08**: `Almacenes/Show.tsx` y `Productos/Edit.tsx` tienen **0 instancias** de `<Combobox>` (confirmado por grep). El resto de la lista vieja (`Vendor/Show.tsx`, `Vendor/Listado.tsx`, `Comprar/Index.tsx`, `Productos/Vendor/Index.tsx`) ya tiene bastante Combobox (6 a 46 instancias cada uno) — antes de ofrecerlos de nuevo, confirmar si el `<Select>` residual que queda en cada uno es un caso legítimo (filtro simple) o de verdad el patrón viejo sin convertir.
- [ ] **F3** — Mover configuración de cuenta mensajero de `Almacenes/Edit` a `Empleados/Edit` (reverificado 2026-09-08: `mensajero_cuenta_id` sigue solo en `Almacenes/Edit.tsx`).
- [ ] **`Cierres/Create.tsx` — modal de confirmación inalcanzable**: existe el state `showConfirmModal` y su `AlertDialog` ("¿Finalizar Cierre?"), pero el botón "FINALIZAR CIERRE" llama `submit()` directamente en el `onClick` — nunca hace `setShowConfirmModal(true)`. El cierre se finaliza sin pedir confirmación, aunque el modal está construido y listo. Encontrado 2026-07-31, reverificado sin cambios 2026-09-08.
- [ ] **`Cierres/Create.tsx` — código muerto**: bloque `{false && (...)}` de ~80 líneas ("Distribución del Dinero", cuentas vs clientes) que nunca se renderiza. Encontrado 2026-07-31, reverificado sin cambios 2026-09-08.
- [ ] **`Cierres/Create.tsx` / `Cierres/Show.tsx` — duplicación masiva**: ambos archivos (~2310 líneas cada uno) comparten casi todo el JSX (widgets, tablas, diálogos, comparativa) copiado y pegado, y declaran por separado el mismo set grande de interfaces TypeScript (`Calculos`, `DetalleMoneda`, `ItemVenta`, `ItemMovimiento`, `TransferenciaItem`, etc. — sin archivo de tipos compartido). `Show.tsx` además reconstruye a mano un objeto `calculos` desde el `cierre` guardado (líneas ~400-470) solo para reutilizar el render de `Create.tsx`. Candidato a extraer componentes/tipos compartidos. Encontrado 2026-07-31, no revisado a fondo en la pasada del 2026-09-08.
- [ ] **Idea del cliente: sacar el precio de costo de un producto a su propio modelo** — hoy `precio_compra_producto` es una columna plana en `Producto`. Pedido 2026-08-20 mientras se cerraba el módulo Distribución de Costos, "para luego". El 2026-08-22 volvió mucho más desarrollado como `docs/modelo-costo-producto/prompt-general.md`: un módulo completo de "Formación de Costos y Precios" (ficha versionada por producto, conceptos configurables, motor de cálculo desacoplado, multi-moneda, plantillas, aprobación, dashboard). Solo analizado a nivel de integración (qué reusar: `Moneda`, `HistorialPrecioCosto`, `ProductoVendedor`, el patrón de Distribución de Costos; qué es nuevo: `cost_sheets`/`cost_sheet_items` versionados + un motor de cálculo puro sin Eloquent) — **no implementar sin aprobación explícita del cliente**, el documento mismo lo pide. Decisiones ya resueltas 2026-08-22: (1) **convive** con `precio_compra_producto`, no lo reemplaza (razón del cliente: proyecto con presión competitiva real, prioridad es entregar valor rápido sin arriesgar lo que ya funciona — ver memoria `project_contexto_negocio_prioridad_cliente`); (2) **vive dentro de Inventario** (`/disponibles`), no como sección/menú aparte — razón del cliente: los responsables de punto de venta necesitan esa documentación a mano donde ya trabajan. Conexión encontrada: `MovimientosController::recibir()` nunca crea precio de venta al recibir un producto en un almacén nuevo, de forma silenciosa — 3 opciones ofrecidas al cliente (prorrateo de costo en Movimientos / aviso de "sin precio" ligado a entregas / cerrar el bypass de venta especial que hoy permite vender sin `ProductoVendedor`), sin elegir todavía. Ver memoria `project_costo_promedio_ponderado` para el análisis completo.
- [ ] **`lotes_stock` — sin vista de consulta + riesgo de desincronización** (encontrado 2026-09-07, junto con el módulo de estados de Compra descrito abajo en "Historial"): no existe ninguna pantalla (ficha de Producto, Inventario Disponible, reportes) para consultar los lotes — el dato solo es accesible por SQL directo, confirmado 2026-09-08 (0 archivos frontend con "lote" en el nombre). Riesgo real sin resolver: `DistribucionCostosController` actualiza `Producto.precio_compra_producto` al repartir un costo adicional pero no toca `lotes_stock.precio_costo` (confirmado por grep, 0 referencias a `LoteStock` en ese controller) — un lote puede quedar con el costo viejo desincronizado del producto después de una distribución de costos.
- [ ] **Documentación vs. código — trabajo sin documentar, pregunta pendiente al cliente**: `app/Services/DetalleOperacionService.php` (316 líneas) y `resources/js/components/detalle-operacion.tsx` (635 líneas) existen en el código y están en uso real (inyectados en `CuentaController`, usados en `Clientes/Show.tsx`, `Proveedores/Show.tsx`, refactor de `RastreoOperacionesController`) pero no aparecen en ningún doc de `docs/` ni fueron explicados por el cliente cuando se le preguntó — ver memoria `project_backlog_reconciliado_2026_09_07`. No asumir que es seguro tocar `Cliente/Proveedor/Cuenta Show.tsx` o `RastreoOperacionesController` sin volver a preguntar primero qué fue este trabajo.
- [ ] **Unificar `PAYMENT_VIAS` (Ventas) con `cuentas.tipo_banco` (Cuentas) — solo analizado, nada implementado, 2026-09-12**: hoy son 2 catálogos de "banco/método" desconectados. `resources/js/components/ventas/PaymentForm.tsx:61-71` tiene una lista hardcodeada (`zelle, cashapp, visa, mastercard, stripe, paypal, qvapay, enzona, transfermovil`) para el campo `pago_ventas.via_pago`, que **no** existe en `CatalogoTarjetasService` (Transfermóvil/EnZona/Stripe/PayPal/QvaPay faltan ahí). Además `'pagos.*.via' => 'nullable|string'` (`VentaController.php:889` y `:1764`) no tiene whitelist ni se cruza contra la cuenta elegida — un vendedor puede elegir cuenta "BANDEC" y vía "Zelle" sin que el sistema lo detecte. Punto de extensión natural: `getCuentasFiltradas()` (`VentaController.php:284-324`) solo filtra por moneda+tipo hoy. Antes de tocar código hay que decidir: (1) ampliar el catálogo de bancos con los métodos cubanos/internacionales que faltan, (2) si `via_pago` se deriva de `tipo_banco` o conviven los dos. Ver memoria `project_unificacion_via_pago_tipo_banco`.
- [ ] **`ProductoImport.php` — importación de Excel no audita cambios de costo (`precio_compra_producto`)**: a diferencia de la edición manual (`ProductoController::update()`, que crea `HistorialPrecioCosto` y exige contraseña admin/moderador cuando cambia el costo), el import actualiza el costo de productos existentes directo (línea ~126-129), sin contraseña y sin dejar rastro en `HistorialPrecioCosto`. Encontrado 2026-09-10 revisando el archivo antes de la primera importación real de productos del cliente. **A propósito no se toca todavía** — el cliente pidió esperar a que se complete esta primera importación real antes de agregar el registro de auditoría, para no arriesgar el proceso en curso. Riesgo menor detectado en el mismo repaso: `categorias.nombre_categoria` es `VARCHAR(20) UNIQUE` — una categoría del Excel con nombre más largo de 20 caracteres hace que esa fila se omita silenciosamente (queda solo en el log, no en el mensaje final que ve el usuario).

### Baja prioridad / limpieza
- [ ] **`ProductoVendedorController::setPreciosBase()` — código muerto confirmado, 2026-09-15**: método backend + ruta (`disponibles.precios-base`, POST) + columnas `producto_vendedors.precio_admin`/`ganancia_admin` (con su propia migración) existen completos, pero **ningún archivo del frontend los llama** (confirmado por grep en `resources/js/`, 0 resultados). Era un "precio base de referencia" separado del `precio_venta` real que nunca se conectó a ninguna pantalla. Decidir si se elimina o se conecta — no tocar sin que el cliente decida.
- [ ] **`Cierres/Create.tsx` — no existe conteo físico real de caja**: `saldo_contado` se inicializa como `calculos.saldo_esperado_global` y **no hay ningún `<Input>` en el formulario que permita al usuario escribirlo** (se verificó: no hay `setData('saldo_contado', ...)` en todo el archivo). Consecuencia: `saldo_contado` siempre queda igual a `saldo_esperado`, y por lo tanto `diferencia` (calculada en `store()`) **siempre es 0**. El concepto central de un cierre de caja (comparar lo contado físicamente contra lo esperado) no está implementado en la UI, aunque el backend sí lo soporta completo. Encontrado 2026-07-31. **Bajado de Alta a Baja prioridad el 2026-08-22 — el cliente confirmó explícitamente que no van a usar un contador de caja por ahora**, no ofrecer esto como urgente salvo que lo pida. Reafirmado 2026-09-09 al rechazar explícitamente un filtro/badge de "Cuadre" en `Cierres/Index.tsx` por la misma razón (sin conteo real, `diferencia` no es un dato confiable) — no ofrecer tampoco ese filtro hasta que se resuelva esto primero.
- [ ] **Cierre de Caja no captura `turno_vendedor_id`** — a diferencia de Venta/Gasto/Ingreso/Transferencia/Compras (todas capturan turno desde 2026-09-09), `cierre_cajas` no tiene la columna ni `CierreCajaController` la captura. Además, un Cierre cubre un RANGO de tiempo que puede incluir varios empleados relevándose en la misma cuenta (con el fix de re-captura-por-login, ver historial) — un diseño ingenuo de "una sola FK" sería engañoso. Diseño ya discutido con el cliente y explícitamente pospuesto a baja prioridad: (1) `turno_vendedor_id` simple = quién cerró la caja, (2) lista derivada (no columna nueva) de nombres distintos en Venta/Movimiento dentro del rango del cierre = quiénes operaron durante el período. Ver memoria `project_atendido_por_idea`.
- [ ] **Combobox — MEDIUM prioridad**: `Empleados/Create.tsx`, `Empleados/Edit.tsx`, `Reportes/Report/RastreoOperaciones.tsx`, `Logistica/layout/ComprasVentas.tsx`, `Logistica/layout/ProductosPorAlmacen.tsx`. No reverificado en la pasada del 2026-09-08. (`Transacciones/CambiarCostoManual.tsx` ya no existe — el módulo se movió y reescribió como `DistribucionCostos/CambiarCostoManual.tsx`, 2026-08-20, que ya usa el Combobox validado.)
- [ ] **Rutas rotas**: `POST /movimientos/{movimiento}/aprobar` (registrada en `routes/acciones/movimientos.php:28`) sin método `aprobar()` en `MovimientosController` — reverificado 2026-09-08, sigue así. Implementar o eliminar la ruta.
- [ ] **Ruta huérfana**: `routes/vendor/vendedor.php` no está incluida desde `routes/web.php` (ni desde ningún otro archivo de rutas) — reverificado 2026-09-08. Incluirla o eliminarla.
- [ ] **F2** — Display mensajero multi-moneda completo (hoy limitado a USD/CUP).
- [ ] **F4** — `ganancia_real_total` muestra 0 en algunos casos. No revisado a fondo en la pasada del 2026-09-08 (el campo sí se usa en varios puntos de `VentaController.php`, ej. líneas 588/1486/1655, pero no se reprodujo el caso del bug).
- [ ] **`Clientes/Index.tsx` — diálogo "No se puede eliminar" con colores desalineados**: usa `color === 'red' ? ... : 'green'/gris`, mismo patrón viejo que ya se corrigió (a rojo/ámbar/esmeralda) en la tabla de la misma página. Encontrado 2026-08-22, señalado al cliente, no resuelto — confirmar antes de tocar.
- [ ] **`Movimientos/Show.tsx` — badge opcional de prorrateo pendiente**: cuando un movimiento específico tiene `requiere_prorrateo=true` y `prorrateo_decision` nulo, mostrar un aviso corto (solo admin/moderador) con link directo a la pestaña "Movimientos" de Distribución de Costos. Ofrecido al cliente al cerrar `prorrateo-costos-movimientos-2026-08-25.md`, no confirmado todavía — no implementar sin que lo pida.
- [ ] **`anularVenta` desde `rechazada` — nota de defensa en profundidad, no bug activo**: el método técnicamente no tiene guardia explícita contra revertir el stock dos veces (ya se revirtió en `rechazarSolicitudEspecial`), pero verificado 2026-09-08 que `Vendor/Show.tsx` nunca expone el botón "Anular" para una venta en estado `rechazada` (gateado por `isVentaPendiente`) — no alcanzable por el flujo normal hoy.
- [ ] **`Monedas/Edit.tsx` y `MonedaController::update()` — mismo bug de `commission` marcado obligatorio que se corrigió en `Create.tsx`**: la columna `commission` es `decimal(8,4)->default(0)` (no obligatoria), pero `update()` sigue con `'commission' => 'required|numeric|min:0'` y el label de `Edit.tsx` sigue mostrando el asterisco rojo. Encontrado 2026-09-14 al corregir el mismo problema en `Create.tsx`/`store()` (cambiado a `nullable`, insertando `?? 0` cuando viene vacío) — no se tocó `update()`/`Edit.tsx` todavía, queda pendiente confirmar si se corrige igual.

---

## ✅ Módulos completos y estables

| Módulo | Descripción |
|---|---|
| Productos | CRUD, códigos de barras automáticos, import/export Excel, historial de costos; **cambiar el precio de costo es admin-only desde 2026-09-15** (antes admin+moderador), sigue exigiendo la contraseña real del usuario (`Hash::check`, nunca fue hardcodeada) |
| Almacenes | CRUD, inventario por almacén, asignación de usuarios |
| Compras | Registro, pagos múltiples, distribución de costos, relación proveedor/cliente, máquina de estados (pendiente/aprobada/anulada), edición y anulación con auditoría, trazabilidad por lote (`lotes_stock`, 2026-09-07). **Acceso abierto a moderador 2026-09-09** (antes admin-only) — paridad total con admin, incluye aprobar/anular/editar y captura `turno_vendedor_id` |
| Movimientos de stock | Traslados entre almacenes, estados, discrepancias, auditoría |
| Categorías / Proveedores / Clientes | CRUD completo |
| Cuentas financieras | CRUD, control de saldo con contraseña, tipos de instrumento, historial de operaciones (Compras/Ventas/Transacciones) con filtros, acceso de vendedor a sus propias cuentas (2026-08-01); listado en grilla de "tarjetas de banco" con logo real, filtro por banco, y header con efecto bleed en Create/Edit/Show (2026-09-08); cuentas tipo efectivo con insignia real por moneda (USD/CUP/EUR), mismo mecanismo (2026-09-09); **columna `tipo_banco` nueva (2026-09-12), independiente de `imagen`, para restringir vendedores por banco a futuro — sin implementar todavía la restricción en sí** |
| Monedas y tasas | CRUD, historial de cambio de tasa, impacto en cuentas; **insignia visual por moneda (`monedas.imagen`, catálogo USD/CUP/EUR/MXN/BRL) completa en las 4 vistas** — Edit.tsx/Index.tsx/dashboard/`TasasFlotante.tsx` (2026-09-12), Create.tsx/Show.tsx (2026-09-14, ver historial) |
| Transacciones financieras | Gastos, ingresos, transferencias bidireccionales, **Remesas** (Entrada/Salida/Mensajero, admin/moderador-only, sin cuadre entre montos, 2026-09-14, ver historial), historial embebido en Cuentas/Clientes/Proveedores. **Anulación con motivo de lista fija (2026-09-14)** en las 4 (Gasto/Ingreso/Transferencia abierta a todos los roles con chequeo de cuenta asignada; Remesa admin/moderador-only) — reversión de saldo por delta, ver historial |
| Reportes | Inventario, ventas, compras, ganancias, historial, rastreo |
| API pública | Catálogo sin autenticación (`/api/tienda/*`) |
| Notificaciones | Base de datos, bell en UI, lectura y marcado |
| Ecommerce (básico) | Vista de catálogo pública — en desarrollo |
| "Atendido por" / Turnos | Captura obligatoria (moderador/vendedor) con bloqueo de escrituras sin turno de hoy, indicador de header, cambio a mitad de turno, enlazado a Venta/Gasto/Ingreso/Transferencia/Compras y visible en Ticket + Rastreo de Operaciones + Detalles de Pago (2026-09-08/09). **Re-captura obligatoria en cada login (2026-09-09)** — cuentas compartidas por punto de venta, un relevo de persona vuelve a exigir el diálogo aunque ya hubiera turno capturado ese día |

---

## ✅ Módulo de Ventas — Implementado

| Feature | Estado |
|---|---|
| POS — crear venta con carrito | ✅ |
| Multi-moneda en pagos | ✅ |
| Comisión vendedor por item | ✅ |
| Venta especial (precio bajo mínimo) | ✅ |
| Aprobación/rechazo venta especial | ✅ |
| Venta con gestor (intermediario) | ✅ |
| Pago a cliente (deuda diferida) | ✅ |
| Edición de venta pendiente | ✅ |
| Anulación con motivo y reversión | ✅ |
| **Devolución de venta completada** (estado `devuelta`, distinto de `cancelada`) | ✅ (2026-09-15) |
| Mensajero multi-moneda en POS | ✅ (2026-06-25) |
| Monto final CUP editable en Show | ✅ (2026-06-28) |
| Validaciones al aprobar (mensajero) | ✅ (2026-06-28) |
| Widget TasasFlotante global | ✅ (2026-06-28) |
| Destinatario de venta | ✅ |
| Listado de ventas con filtros | ✅ |

---

## ✅ Módulo de Cierre de Caja — Implementado

| Feature | Estado |
|---|---|
| Cálculo de saldo esperado | ✅ |
| Separación mensajero del saldo | ✅ (2026-06-28) |
| Snapshot de cuentas y clientes | ✅ |
| Snapshot de mensajero | ✅ (2026-07-01) |
| Resumen financiero del turno (tarjeta verde) | ✅ (2026-07-01) |
| Desglose per-venta comisión PV y Gestor | ✅ (2026-07-01) |
| Mensajero en modal "Por dónde entraron" | ✅ (2026-07-01) |
| Comparativa con cierre anterior | ✅ |
| Vista Create.tsx actualizada | ✅ |
| Vista Show.tsx actualizada | ✅ |

---

## ✅ Bot de Telegram — Implementado (rama feature/bot-telegram)

| Feature | Estado |
|---|---|
| Infraestructura: paquete, webhook, secret | ✅ |
| Campo `telegram_chat_id` en usuarios | ✅ |
| Comando `/vincular CODIGO` | ✅ |
| Comando `/start` | ✅ |
| Comando `/reporte` | ✅ |
| Comando `/cierres` | ✅ |
| Comando `/ayuda` | ✅ |
| Notificación cierre de caja | ✅ |
| Notificación venta especial + botones Aprobar/Rechazar | ✅ |
| Notificación decisión venta especial al vendedor | ✅ (database only) |
| Notificación venta creada | ✅ |
| Notificación movimiento de stock | ✅ |
| Notificación cambio precio vendedor | ✅ |
| Notificación movimiento financiero | ✅ |
| Notificación Remesa (creación + anulación) | ✅ (2026-09-14) |
| Notificación anulación Gasto/Ingreso/Transferencia | ✅ (2026-09-14) |
| Notificación Devolución de Venta (`VentaDevueltaNotification`, solo cuando la venta ya estaba completada) | ✅ (2026-09-15) |
| Notificación diaria "cierres pendientes" (`NotificarCierresPendientes`, `dailyAt('21:00')`) | ⚠️ Código en producción, **el cron que dispara `schedule:run` NO está configurado todavía** — ver Bugs/Pendientes abajo |

---

## ✅ Bugs resueltos recientemente

| ID | Método | Descripción | Fix |
|---|---|---|---|
| **B1** | `guardarDistribucion` | Devuelve `saldo_actual` en la respuesta pero el campo real es `saldo_cuenta` | Corregido: ahora retorna `saldo_disponible` desde `saldo_cuenta` (VentaController:L1819) |
| **B2** | `aprobarVenta` | Falta guardia XOR en comisión vendedor | Corregido: `if (!$venta->es_venta_gestor && ...)` en L1374 |
| **B3** | `procesarVenta` | `foreach ($pagos)` sin `?? []` | Corregido: `foreach ($validatedData['pagos'] ?? [] as $pago)` en L803 |
| **B6** | `MovimientosController::rechazar()` | Rechazar un movimiento `en_transito` incrementaba `cantidad` en el origen además de liberar `cantidad_en_transito` — pero `cantidad` nunca se decrementa en `enviar()` (solo en `recibir()`). Duplicaba stock fantasma en cada rechazo | Corregido 2026-07-31: se quitó el `increment('cantidad', ...)` indebido |
| **B7** | Migraciones `estado` enum (`movimientos`, `movimiento_seguimientos`) | Las migraciones que agregan `pendiente_confirmacion` al enum están gateadas a `if (driver === 'mysql')` — en SQLite (default del quickstart) cualquier creación de movimiento fallaba con `CHECK constraint` | Corregido 2026-07-31: nueva migración solo-SQLite (`2026_07_31_141334_fix_movimientos_estado_enum_sqlite.php`), no-op en MySQL |
| **B8** | `TransferenciaController::store()` | El check de permiso de vendedor sobre la cuenta destino usaba `DB::table('user_cuentas')->pluck('cuenta_id')` sin filtrar por el usuario actual — un vendedor podía transferir a la cuenta de OTRO vendedor | Corregido 2026-07-31: `auth()->user()->cuentas()->pluck('id')`, igual patrón que el check de origen |
| **B10** | `MovimientoFinancieroNotification::via()` | Solo comprobaba `role === 'admin'` para decidir si mandar Telegram en Gasto/Ingreso/Transferencia — `moderador` nunca recibía notificación pese a tener acceso total al resto del sistema | Corregido 2026-09-14: `in_array($role, ['admin', 'moderador'])`. Mismo chequeo explícito agregado también a la nueva `RemesaNotification::via()` |
| **B9** | `CuentaController::obtenerHistorialVentas()` | Al agregar filtros (`->where('fuente', ...)`) sobre una query armada con `UNION ALL` embebida como subquery cruda (`DB::raw()`), usar `mergeBindings($query)` y luego encadenar `->where()` corrompe el orden de los bindings — Laravel compila el bucket `where` ANTES que `union`, así que el binding del filtro nuevo se cuela en medio de los bindings del UNION en vez de ir al final (donde está su `?` real en el SQL de texto). Filtrar por "Comisión Gestor" devolvía filas de "Pago de venta" | Corregido 2026-08-01: reemplazar `mergeBindings($query)` por `addBinding($query->getBindings(), 'where')`, que aplana todo en un solo bucket en el orden real del SQL. Regresión cubierta por 2 tests nuevos en `CuentaTest.php` |
| **B11** | `Productos/Edit.tsx` — campo Precio de Costo | El `onChange` hacía `parseFloat(e.target.value) \|\| 0` en cada tecla — al escribir "21." el valor se guardaba como `21` (número, sin el punto) y el input se re-renderizaba sin él, dando la sensación de que no dejaba escribir el punto decimal (pegar sí funcionaba, porque el valor ya venía completo). Reportado por el cliente vía WhatsApp como 🚨 prioridad | Corregido 2026-09-15: el campo guarda el texto tal cual mientras se escribe (validado con regex `^\d*\.?\d*$`) y solo se convierte a número al comparar/enviar |

## 🐛 Bugs activos pendientes

### Infraestructura — cron del Scheduler sin configurar (2026-09-14)

`NotificarCierresPendientes` (y cualquier otra tarea futura en `routes/console.php`) depende de que algo externo llame `php artisan schedule:run` cada minuto. Verificado por SSH en el servidor de producción que **este hosting compartido (Hostinger/`main-hosting.eu`) no permite configurar cron desde la terminal** (no existe el binario `crontab`, y hay un script que lo deja de solo lectura a propósito) — la única vía es entrar a **hPanel → Cron Jobs** desde el navegador. Comando exacto a registrar ahí: `* * * * * php /home/u706356131/domains/TU_DOMINIO/public_html/proyectos/almacen.tienda/artisan schedule:run >> /dev/null 2>&1` (falta confirmar la ruta exacta del dominio real). Hasta que esto se configure, el aviso diario de cierres pendientes **no se va a disparar solo**, aunque el código ya está desplegado. Ver memoria `reference_acceso_servidor_produccion` / `project_notificaciones_telegram_mejoras`.

### Anulación — pregunta del cliente sin responder

Durante la verificación en navegador el cliente preguntó *"¿agregaste un botón para aprobar verdad?"*, sugiriendo que quizás esperaba un flujo `pendiente → aprobada` (como Compras) en vez de "completado al instante + se puede anular" (lo que efectivamente se construyó). Se le explicó la diferencia pero cambió de tema sin responder explícitamente. **El diseño actual está en producción, pero no dar por confirmado que es el final** — volver a preguntar si el tema reaparece. Ver memoria `project_anulacion_operaciones`.

### Mensajero

| ID | Módulo | Descripción | Impacto |
|---|---|---|---|
| **B4** | `AlmacenController@edit` | Carga todas las cuentas (USD, CUP, MLC mezcladas) en el selector de cuenta mensajero | El usuario puede seleccionar una cuenta no-CUP y romper la lógica |
| **B5** | Config mensajero | La cuenta de mensajero se configura en `Almacenes → Edit`. Debería estar en `Empleados → Edit` | UX confusa, admin tiene que ir a dos lugares |

---

## 🔧 Features pendientes

### Show.tsx (Vendor/Show.tsx)

| ID | Descripción | Prioridad |
|---|---|---|
| **F2** | Display mensajero multi-moneda completo — la interfaz TypeScript y el display deben soportar cualquier moneda del sistema, no solo USD/CUP | Media |

### Empleados

| ID | Descripción | Prioridad |
|---|---|---|
| **F3** | Mover configuración de cuenta mensajero de `Almacenes/Edit` a `Empleados/Edit` — el admin asigna el almacén y configura la cuenta mensajero en el mismo formulario | Media |

### Ganancia

| ID | Descripción | Prioridad |
|---|---|---|
| **F4** | `ganancia_real_total` muestra 0 en algunos casos — separar correctamente el cálculo | Media |

---

## ✅ Features completadas recientemente

| ID | Descripción | Fecha |
|---|---|---|
| **—** | **Módulo Transacciones (75%)** — Gastos, Ingresos, Transferencias con control de saldo, arreglo flujo contable. ⚠️ Sin cambios desde esta fecha: los 8 commits del 2026-07-30 titulados "Mejorando transacciones sections" en realidad modificaron `CierreCajaController`/`Cierres/*.tsx`, no los controladores de Transacciones — no leer el git log literalmente para este módulo. | 2026-07-28 |
| **—** | **Cuentas — tipo_titular** (externa/personal), eliminación campo `deuda`, unificación `temporales→permanentes` | 2026-07-28 |
| **—** | **Cuentas — soporte saldos negativos** detectados como deudas, ajustes en UI | 2026-07-28 |
| **—** | **Logística II** — Expansión con Create/Edit/Show, layouts de charts (ComprasVentas, ProductosPorAlmacen) | 2026-07-27 |
| **—** | **Logística I** — 4 widgets Capital Financiero (Capital, USD, CUP, EUR) con datos reales, resúmenes de Cuentas/Clientes/Proveedores/Productos | 2026-07-26 |
| **F6** | Herramienta detección y fusión de productos duplicados — 2 modales, agrupación con capacidad normalizada, suma cantidades + promedio precios | 2026-07-25 |

## 📋 Pendientes menores conocidos

- [ ] El comando `/reporte` del bot no filtra por almacén del usuario — devuelve todos los almacenes. Considerar filtro para admins con almacenes asignados.
- [ ] Ecommerce: vista pública del catálogo está en desarrollo, no vinculada al POS.
- [ ] **Reportes — prioridad bajada explícitamente por el cliente 2026-09-05**: los 14 reportes restantes se trabajan solo bajo petición, no proactivamente. Ver `docs/arreglos-pendientes/reportes-arreglos-2026-08-01.md` (índice con hallazgos por reporte). **Rastreo de Operaciones** (el que más le interesaba al cliente) tiene Fases 0-3, 5, 7, 8, 9 cerradas, más el fix de `exportToPDF` del 2026-09-05; quedan Fase 4 (stock final) y parte de Fase 6 (drill-down), ambas baja prioridad — detalle en `docs/arreglos-pendientes/rastreo-operaciones-rediseno-2026-08-01.md`. **Bug real confirmado 2026-09-08**: `Reportes/Report/ProductosMasVendidos.tsx` existe como archivo pero tiene **0 líneas** — el reporte se rompe al abrirlo. Varios reportes siguen sin paginación/límite.

---

## 📁 Archivos clave por módulo

### Ventas
```
app/Http/Controllers/VentaController.php     ← controlador principal (más de 1800 líneas)
app/Models/Venta.php
app/Models/VentaDetalle.php
app/Models/PagoVenta.php
resources/js/pages/Vendor/Index.tsx          ← POS
resources/js/pages/Vendor/Show.tsx           ← gestión de venta pendiente
resources/js/pages/Vendor/Listado.tsx        ← listado con filtros
```

### Cierre de Caja
```
app/Http/Controllers/CierreCajaController.php    ← ~1,943 líneas
app/Models/CierreCaja.php
resources/js/pages/Cierres/Create.tsx
resources/js/pages/Cierres/Show.tsx
resources/js/pages/Cierres/Index.tsx
```

### Bot Telegram
```
app/Http/Controllers/TelegramWebhookController.php
app/Channels/TelegramChannel.php
app/Notifications/CierreCajaNotification.php
app/Notifications/VentaEspecialSolicitudNotification.php
app/Notifications/VentaEspecialDecisionNotification.php
app/Notifications/VentaCreadaNotification.php
app/Notifications/MovimientoStockNotification.php
app/Notifications/MovimientoFinancieroNotification.php
app/Notifications/CambioPrecioVendedorNotification.php
config/telegram.php
routes/api.php                               ← ruta /telegram/webhook
```

### Precios por vendedor
```
app/Http/Controllers/ProductoVendedorController.php
app/Exports/PreciosVendedorExport.php
app/Imports/PreciosVendedorImport.php
resources/js/pages/Productos/Vendor/Index.tsx
```

### Usuarios y empleados
```
app/Http/Controllers/UserController.php
app/Http/Controllers/UserAlmacenController.php
resources/js/pages/Empleados/Index.tsx
resources/js/pages/Empleados/Create.tsx
resources/js/pages/Empleados/Edit.tsx
```

### Logística
```
app/Http/Controllers/LogisticaController.php
app/Services/DashboardStatsService.php
resources/js/pages/Logistica/Index.tsx
resources/js/pages/Logistica/Create.tsx
resources/js/pages/Logistica/Edit.tsx
resources/js/pages/Logistica/Show.tsx
resources/js/pages/Logistica/layout/*         ← ComprasVentas, ProductosPorAlmacen, KPIsPeriodo
```

### Transacciones
```
app/Http/Controllers/TransaccionController.php
app/Http/Controllers/GastoController.php
app/Http/Controllers/IngresoController.php
app/Http/Controllers/TransferenciaController.php
resources/js/pages/Transacciones/Index.tsx
resources/js/pages/Transacciones/Create.tsx
resources/js/pages/Transacciones/Show.tsx
resources/js/pages/Transacciones/Historial.tsx
resources/js/pages/Transacciones/CambiarCostoManual.tsx
resources/js/pages/Transacciones/layout/*      ← Movimientos, forms varios
```

---

## 🔄 Historial de cambios recientes

| Fecha | Cambio |
|:---:|---|
| 2026-09-15 | **Aprobación de venta — permitir saldo negativo en cuenta de Comisión/Gestor.** El cliente pidió que aprobar una venta pueda decrementar la cuenta de comisión del vendedor y la cuenta del gestor aunque no tengan saldo suficiente, dejándolas en deuda — mismo criterio que ya aplicaba al mensajero (que nunca tuvo ese chequeo). Quitados los 2 `throw` bloqueantes en `VentaController::aprobarVenta()` (gestor y comisión) que antes abortaban toda la transacción cuando `saldo_cuenta < monto`. En `Vendor/Show.tsx`, el botón "Aprobar Venta" ya no se deshabilita por falta de fondos (`gestorSinSaldo`/`comisionSinSaldo` quitados de `puedeAprobar`) — el aviso rojo/verde en las tarjetas de resumen se mantiene, ahora es solo informativo, no bloqueante. 2 tests nuevos en `VentaTest.php` (comisión y gestor con saldo insuficiente → aprueban y la cuenta queda en negativo). **Verificado end-to-end en el navegador**: venta de prueba (#33, CONECTOR $7, comisión $1 = 690 CUP) sobre la cuenta `DANIEL CUP` con saldo 0,00 — el indicador rojo se mostró pero no bloqueó el botón, la aprobación quedó COMPLETADA, la cuenta confirmada en BD en -690, y luego revertida con la propia función de Devolución de la app (cuenta de vuelta a 0,00, venta queda `devuelta`) para no dejar dato de prueba corrompiendo saldos reales de desarrollo. |
| 2026-09-15 | **Sesión larga: rename "Remesas"→"Operaciones Múltiples" + módulo nuevo "Devolución de Venta" + fixes de Productos.** (1) **Rename de texto "Remesas"→"Operaciones Múltiples"** — solo lo visible en pantalla (botón/pestaña/títulos/badges en `Transacciones`, `Cuentas/Clientes/Proveedores Show.tsx`, notificación de la campanita), sin tocar nombres de variables/rutas/tabla `remesas`/`RemesaController` ni el mensaje de Telegram. Incluye las etiquetas que generaba el backend (`CuentaController::obtenerHistorialRemesas()`, antes `'Remesa (Entrada)'` etc.) — 1 test actualizado (`RemesaHistorialTest.php`). La carpeta muerta `resources/js/pages/Remesas/` y el widget muerto `WidgetInventario.tsx` (ambos ya no relacionados con el módulo real) quedaron sin tocar a propósito. (2) **Devolución de Venta — módulo nuevo.** El cliente aclaró la regla real: anular solo aplica a una venta **pendiente** (nunca movió dinero); una venta **completada** que se revierte es conceptualmente una Devolución, aunque el mecanismo de reversión sea idéntico al de `anularVenta()` (revierte 100% — stock, cuentas, comisión, gestor, mensajero — nunca parcial por línea, decisión explícita del cliente). Implementado: nuevo estado `devuelta` en el enum de `ventas.estado` (migración nueva, gateada a MySQL igual que los estados especiales anteriores), `anularVenta()` decide el estado final según si la venta era `pendiente` (→ `cancelada`, sin cambios) o `completada` (→ `devuelta`, nuevo) antes de que la transacción cambie el valor. Frontend (`Vendor/Show.tsx`): el botón/diálogo/banner dicen "Devolución" en vez de "Anular Venta" cuando la venta está completada — **antes de esta sesión no existía ningún botón en la UI para revertir una venta ya completada**, pese a que el backend ya soportaba la reversión completa. `Vendor/Listado.tsx` y `Clientes/Show.tsx` ganan badge naranja "Devuelta" + filtro de Estado nuevo. `DetalleOperacionService`/Rastreo de Operaciones y `detalle-operacion.tsx` muestran el motivo también para `devuelta` (antes solo `cancelada`) — se agregó `info_general.estado` al shape de `DetalleVenta` para poder distinguir el título de la card ("Venta Devuelta" vs "Venta Anulada"). Notificación Telegram nueva (`VentaDevueltaNotification`, admin/moderador) — **disparada solo cuando es una Devolución real, nunca al anular una venta pendiente** (que no mueve dinero). 8 tests nuevos + 1 actualizado. **Verificado end-to-end en el navegador** con una venta real de prueba (`QA-TEST DEVOLUCION`, venta #32, $36, `OLLA ARROCERA` en `QUIVICAN TIENDA`): se encontró y corrigió en el camino que la migración nueva nunca se había corrido en la base de datos de desarrollo (`php artisan migrate`), y que el badge naranja de `Vendor/Listado.tsx` no se había implementado todavía (se agregó en el momento) — confirmado en BD que el stock y el saldo de la cuenta de comisión volvieron exactos al valor original. **Pendiente**: la Fase 5 (sección "Ventas Devueltas" separada en el resumen del Cierre de Caja) no se implementó todavía, y la venta #32 de prueba sigue en la BD de desarrollo — ver Media prioridad arriba. (3) **Productos — 2 fixes pedidos por el cliente.** Cambiar el precio de costo ahora es **admin-only** (antes admin+moderador), en frontend y backend. **Bug real reportado por WhatsApp con prioridad 🚨**: el campo de precio de costo no dejaba escribir el punto decimal (solo pegar funcionaba) — causa real encontrada: `parseFloat(e.target.value) \|\| 0` en cada tecla borraba el "." en cuanto no había dígitos después, y el input se re-renderizaba sin él. Corregido guardando el texto tal cual mientras se escribe (regex `^\d*\.?\d*$`), convirtiendo a número solo al comparar/enviar. 5 tests nuevos (`ProductoTest.php`, no existía ningún test para `ProductoController::update()` hasta ahora). (4) **Analizado `ProductoVendedorController.php`** a pedido del cliente: confirmado que la actualización masiva de precio en varios almacenes (`updateBulk()`, modal en `/disponibles`) **ya permite cambiar la comisión también** (backend y frontend, campo "Comisión (USD, opcional)" ya existía) — se le puede confirmar al cliente que esto ya estaba implementado. Hallazgo de paso: `setPreciosBase()`/`precio_admin`/`ganancia_admin` es código muerto confirmado (ruta y columnas existen, cero uso en frontend) — agregado a Baja prioridad. Suite completa: **376/376 tests verdes**. Ver memorias `project_devolucion_venta`, `project_remesas_modulo` (actualizada), `project_producto_precio_costo_fixes`. |
| 2026-09-14 | **Sesión larga, módulo nuevo "Remesas" + anulación en Transacciones + notificaciones Telegram — desplegado a producción.** (1) **Módulo Remesas**: 4ta pestaña en Transacciones (junto a Gasto/Ingreso/Transferencia), admin/moderador-only, 3 patas independientes (Entrada/Salida/Mensajero opcional) sin cuadre entre montos (la diferencia es el margen del negocio) + Notas libres. Backend: tabla `remesas`, `RemesaController` (`formData`/`store`/`show`/`anular`), rutas bajo middleware `admin`. Frontend: `RemesaForm.tsx` (mismo patrón que `TransferenciaForm.tsx`), `Transacciones/RemesaShow.tsx` (página de detalle dedicada, sileo para flash). Historial embebido en `Cuentas/Show.tsx` (nueva card), `Clientes/Show.tsx` (5ta pestaña) y `Proveedores/Show.tsx` (3ra pestaña), reusando `DetalleOperacionService` con un nuevo método `detalleRemesa()` (patrón `movimientos_saldo`, igual que Venta/Compra, por tener 3 patas en vez de origen/destino binario). Las 3 páginas ocultan la sección completa para vendedor. **Bug real encontrado en el camino:** faltaba `use Inertia\Inertia;` en `RemesaController.php`, tiraba 500 real (atrapado por el primer test de `show()`). 14 tests nuevos (`RemesaTest.php` + `RemesaHistorialTest.php`). (2) **Anulación de Gasto/Ingreso/Transferencia/Remesa** — pedido explícito del cliente ("agregar un estado para poder eliminar en caso de error"). Diseño confirmado por el cliente: **todos los roles pueden anular** Gasto/Ingreso/Transferencia (mismo criterio de cuenta-asignada que ya existe para crear; Remesa se dejó admin/moderador-only, igual que el resto de esa feature) y **motivo de lista fija** (no texto libre): `error_monto`/`error_entidad`/`duplicado`/`error_tipo_operacion`/`solicitud_cliente`/`otros` (este último exige detalle). Reversión de saldo **por delta** (`saldo_anterior - saldo_posterior` con `increment()`, no restaurando al snapshot exacto) — correcta aunque hayan ocurrido otras operaciones sobre la misma cuenta después; verificado con test dedicado. Rechaza anular movimientos sin snapshot de saldo (anteriores a 2026-08-01). `AnularOperacionDialog.tsx` (componente compartido) en `Transacciones/Show.tsx` y `RemesaShow.tsx`. **Pregunta del cliente sin responder** sobre si debía llevar un paso de aprobación (`pendiente→aprobada`, como Compras) — ver "Bugs activos pendientes" arriba, no dar el diseño actual por confirmado como final. 9 tests nuevos (`AnulacionOperacionesTest.php`). (3) **Notificaciones Telegram** — `RemesaNotification` nueva (creación + anulación, admin/moderador con chequeo explícito de rol); `MovimientoFinancieroNotification` gana 4to parámetro `$anulado` (título "ANULADO:", ícono ⛔, muestra motivo); **ni Venta ni Compra notificaban su propia anulación por Telegram antes de hoy** — patrón nuevo. **Bug real encontrado y corregido**: `MovimientoFinancieroNotification::via()` solo comprobaba `role === 'admin'`, moderador nunca recibía Telegram en Gasto/Ingreso/Transferencia (ver B10 arriba). 4 tests nuevos (`NotificacionAnulacionTest.php`). (4) **`NotificarCierresPendientes`** (comando Artisan nuevo) + `CierresPendientesNotification` — pedido explícito del cliente: avisar a admins qué usuarios con rol moderador/vendedor no registraron su Cierre de Caja hoy. Criterio final, corregido varias veces por el cliente durante el diseño: **por usuario registrado con ese rol, sin ningún filtro de turno** (`User::whereIn('role', ['moderador','vendedor'])` completo, cruzado contra `CierreCaja` de hoy) — el cliente rechazó explícitamente cruzar con quién capturó turno ese día. Programado en `routes/console.php` (`dailyAt('21:00')`, hora sin confirmar con el cliente). 3 tests nuevos (`NotificarCierresPendientesTest.php`). **Pendiente crítico sin resolver**: el cron que dispara `schedule:run` no está configurado en el hosting de producción — ver "Bugs activos pendientes" arriba. (5) Migración del toast casero (`useState`+div flotante) de los 3 forms de Transacciones a `sileo`, cerrando el último rezago de esa migración. Suite completa: **366/366 tests verdes**. Deploy real a producción confirmado por el cliente (git pull + migrate en vivo vía SSH). Ver memorias `project_remesas_modulo`, `project_anulacion_operaciones`, `project_notificaciones_telegram_mejoras`, `reference_acceso_servidor_produccion`, `reference_analisis_modulo_transacciones` (este último documenta 3 hallazgos de Transacciones dejados sin tocar a propósito: hueco de permisos vendedor/cliente-proveedor, código muerto, inconsistencias de arquitectura frontend). |
| 2026-09-14 | **`Monedas/Create.tsx` y `Monedas/Show.tsx` — cierre del ítem de Alta prioridad, insignias visuales de Monedas 100% completo en las 4 vistas.** **Create.tsx**: campo `imagen` en el `useForm`, prop `catalogoImagenes` consumida (el backend ya la mandaba), `SelectorImagenEfectivo` en el formulario, preview bleed en el banner (quitado `overflow-hidden`), degradado violeta en sus 2 Cards — mismo patrón exacto ya validado en `Edit.tsx`, sin cambios de backend (`store()`/`Moneda::fillable` ya aceptaban `imagen`). **Show.tsx**: `MonedaController::show()` ahora manda `imagen`/`imagen_url` (mismo patrón que `index()`), insignia real a bleed en el banner reemplazando el ícono `Coins` fijo, degradado violeta en sus 5 Cards (Información General/Configuración y Estado/Información del Sistema/Resumen de Estado/Acciones Rápidas). Botón "Volver" de `Show.tsx` y "Cancelar" de `Create.tsx` sin tocar, ya estaban bien ubicados. **Bug real encontrado y corregido en el camino:** el campo `commission` se marcaba obligatorio (`required`) en frontend y backend (`store()`), pero la columna es `decimal(8,4)->default(0)` — no es obligatoria. Corregido a `nullable` en la validación de `store()` (insertando `?? 0` si viene vacío) y quitado el asterisco rojo del label en `Create.tsx`. **Mismo bug sigue sin corregir en `update()`/`Edit.tsx`** — agregado a Baja prioridad, pendiente de confirmación. 1 test nuevo (`MonedaTest.php`), 6/6 verdes en ese archivo. Ver memoria `project_monedas_insignias_visuales`. |
| 2026-09-12 | **Sesión larga, 4 frentes.** (1) **Bug real corregido en `CompraController.php`**: comprar a crédito seleccionando un Cliente registrado (en vez de Proveedor) como `tipo_proveedor` incrementaba `deuda_pago_cliente` en vez de decrementar (4 puntos: `procesarPagos()` deuda total/parcial, `revertirEfectosMonetarios()`, `anular()` fondo) — corregido, 4 tests actualizados/nuevos, verificado en navegador con compra+anulación real. Saldos históricos ya afectados (ALBERTO id 1, FORTE id 19) **no se tocaron a propósito** — el cliente los ajusta a mano en producción. Ver memoria `project_bug_signo_deuda_cliente_como_proveedor`. (2) **`CatalogoTarjetasService` — banco "Square" agregado** (imagen convertida a webp, sin recorte, ya venía en formato badge). (3) **`cuentas.tipo_banco`** — columna nueva independiente de `imagen` (migración nullable + `CatalogoTarjetasService::bancos()`/`bancoSlugsValidos()` + validación en `CuentaController` + `<Select>` en Create/Edit, oculto para tipo=efectivo) — pensada para restringir vendedores por banco en Ventas a futuro, sin implementar esa restricción todavía. Verificado en navegador editando 2 cuentas reales (CAPITAL ONE→`venture_x`, ACIELO INVESTMENTS LLC→`zelle`). Análisis inmediato posterior encontró que esto choca con `PAYMENT_VIAS` de Ventas (catálogo separado, ver Media prioridad y memoria `project_unificacion_via_pago_tipo_banco`) — **decisión del cliente: queda como media prioridad, no arrancar sin confirmación**. (4) **`monedas.imagen` — insignia visual por moneda, catálogo USD/CUP/EUR/MXN/BRL** (`CatalogoTarjetasService::monedaImagenes()`, migración nullable, independiente de `cuentas.imagen`). Completado en `MonedaController`+`Monedas/Edit.tsx` (selector + preview bleed en banner + degradado violeta en sus 2 Cards + botón "Volver" reubicado fuera del banner, corrigiendo una violación real de `docs/header-structure.md`) y `Monedas/Index.tsx` (insignia en la tabla + degradado violeta). Extendido también al widget "Información de Monedas" del dashboard (`ReporteController::getMonedas()` + rediseño con bleed image y badges para tasa/comisión) y a `TasasFlotante.tsx` (widget global de tasas en todas las pantallas, mismo mecanismo). **`Monedas/Create.tsx` y `Show.tsx` quedan pendientes — subidos a Alta prioridad explícitamente por el cliente**, ver arriba. Hallazgo de paso: bug real en `user-info.tsx`/`app-header.tsx` (`user.avatar` en vez de `user.avatar_url`, rompía el avatar del sidebar en cualquier ruta anidada de 2+ niveles) — corregido. 335/335 tests verdes durante toda la sesión. Ver memoria `project_monedas_insignias_visuales`. |
| 2026-09-10 | **Revisión de `ProductoImport.php` antes de la primera importación real de productos del cliente (sin cambios de código).** Confirmado que estructuralmente sigue intacto: todas las columnas usadas (`productos`, `categorias`, `almacen_producto`, `producto_codigos`) siguen existiendo igual, y `PlantillaProductoExport.php` (la plantilla real que descarga el cliente) coincide exactamente con lo que el import espera. Dos hallazgos reales, agregados a Media prioridad arriba, **a propósito no resueltos todavía** — el cliente pidió esperar a que se complete esta primera importación antes de tocar el archivo: (1) el import cambia `precio_compra_producto` de productos existentes sin crear `HistorialPrecioCosto` ni pedir contraseña, a diferencia de la edición manual; (2) `categorias.nombre_categoria` es `VARCHAR(20) UNIQUE`, una categoría del Excel más larga omite la fila en silencio. Confirmado también que el import no crea `LoteStock` (trazabilidad por lote, agregado 2026-09-07 solo para Compras) ni `ProductoVendedor` (precio de venta) — ambos son comportamiento esperado, no gaps nuevos. |
| 2026-09-09 | **"Detalles de Pago" (`Vendor/Show.tsx`) — rediseño completo en cards, cerrando la idea parqueada desde 2026-08-14.** El desbloqueo real: la migración de `cuentas.imagen`/`CatalogoTarjetasService` que este mismo día se extendió a "efectivo" (fila siguiente) era exactamente lo que le faltaba a esta idea para poder construirse — ya no dependía de trabajo nuevo. `VentaController::show()` ahora expone `pago.cuenta.tipo`/`banco` (mismo patrón `CatalogoTarjetasService::porSlug()` que `CuentaController`). Frontend: cada pago pasó de una fila de texto plano a una card independiente estilo `CuentaCard.tsx` — header con el logo real de la cuenta (banco o insignia de efectivo) a bleed completo + badge de método (verde=efectivo/azul=transferencia), nombre del destino con mini-imagen+badge, monto principal en badge grande con el símbolo antepuesto, badges de Moneda/Tasa/Vía/Referencia abajo. Iterado en vivo viendo el resultado en el navegador (venta real #406/#46): el primer intento con imagen `object-contain` chica se veía recortada e ilegible, corregido a `object-cover` bleed completo; el primer intento de badges sueltos en grid se reemplazó por la card completa a pedido explícito del cliente. **Bug real encontrado y arreglado en el camino:** `pago.monto`/`monto_equivalente` llegan del backend como *string* (cast `decimal:2` de Eloquent, mismo patrón ya conocido de Proveedores/Clientes) — anteponer el símbolo a mano con `String.prototype.toLocaleString()` lo ignora silenciosamente (ese método existe pero no aplica las opciones de formato); corregido forzando `Number(...)` antes. 322/322 tests verdes. Detalle completo en memoria `project_detalles_pago_rediseno_visual`. |
| 2026-09-09 | **Cuentas tipo "efectivo" — insignias reales por moneda (USD/CUP/EUR), cerrando el hueco que dejó el rediseño de tarjetas de banco.** El cliente trajo 3 imágenes reales y pidió el mismo patrón exacto que bancos: convertidas a WebP con Imagick (`public/projects/efectivo/{usd,cup,eur}.webp`), `CatalogoTarjetasService` ganó un tercer grupo `'efectivo'` (mismo array plano, sin tabla nueva), nuevo componente `SelectorImagenEfectivo.tsx` (mismo patrón botón+Dialog+grilla que `SelectorBancoTarjeta`, sin pestañas por ser solo 3 opciones). `Cuentas/{Create,Edit,Show}.tsx` ganan el campo "Insignia de Moneda" cuando tipo=efectivo; `CuentaCard.tsx` corregido a `object-cover` a bleed completo (un primer intento con `object-contain` se descartó, el cliente pidió que se viera "como las tarjetas"). Hallazgo casual: varias cuentas ya tenían `imagen='cup'` etc. guardado de antemano (datos preparados anticipando esta feature) — empezaron a mostrar su insignia real sin re-editarlas. 322/322 tests. Detalle en memoria `project_cuentas_rediseno_tarjetas_bancarias`. |
| 2026-09-09 | **`Cierres/Index.tsx` — pasada de UX/UI completa.** Tabla envuelta en `Card` con header degradado (amber, patrón estándar del proyecto, antes era un `div` suelto). Columna "Vendedor" renombrada a "Punto de Venta" en toda la página (columna, filtro, placeholder) — confirma que ese campo es la cuenta compartida del punto de venta, no una persona. Badges+iconos en toda la tabla (antes texto plano). Nueva columna "Responsables del Turno": un Cierre cubre un RANGO de tiempo que puede incluir varios empleados relevándose en la misma cuenta (con el fix de re-captura-por-login de abajo) — se deriva en caliente en `CierreCajaController::index()` cruzando Venta+MovimientoFinanciero del período por cierre, sin agregar columna nueva a `cierre_cajas`; admin muestra badge "Administrador" (nunca captura turno, no es un dato faltante) en vez de "Sin registrar". Columna "Monto" nueva usando `saldo_esperado` (no `saldo_contado`, que siempre es 0 — ver Baja prioridad). Paginación ventaneada (mismo patrón que `Vendor/Listado.tsx`/`Cuentas/Index.tsx`). **Filtro "Cuadre" explícitamente rechazado por el cliente** — ya lo soporta el backend pero bloqueado hasta que se implemente el conteo físico real (ver Baja prioridad). Detalle en memoria `project_cierres_index_ux_pass`. |
| 2026-09-09 | **Compras — gap de captura de turno cerrado.** Consecuencia directa del cambio de acceso de la fila siguiente: `compras` no tenía columna `turno_vendedor_id` (la migración de Venta/Movimientos traía el comentario "Compras no la necesita, solo admin las crea, que nunca captura turno" — ese supuesto dejó de ser cierto). Migración nueva + `Compra::turnoVendedor()` + captura en `CompraController::store()` (mismo patrón `$request->user()->turnoActivo()?->id` que Venta/Gasto — para admin da `null` sin ningún chequeo especial de rol) + `DetalleOperacionService::detalleCompra()`/`detalle-operacion.tsx` ahora muestran "Atendido por" también en Compras (antes solo Venta/Movimiento). Verificado en navegador: compra real de moderador con turno activo, confirmado en BD y visible en `/proveedores/{id}`. 2 tests nuevos, 322/322 suite completa. |
| 2026-09-09 | **Turnos — re-captura obligatoria en cada login.** Las cuentas son compartidas por punto de venta, no por persona — antes, una vez capturado el turno del día, no se volvía a preguntar aunque otra persona relevara a la primera vía logout/login en el mismo terminal. Fix: `AuthenticatedSessionController::store()` pone una bandera de sesión en cada login exitoso; `User::requiereCapturaTurno()` ahora exige captura si esa bandera está activa **o** si no hay turno de hoy (se mantiene la lógica vieja, solo se le agregó el OR); `TurnoVendedorController::store()` limpia la bandera al capturar. No se tocó el esquema ni el middleware `RequireTurnoActivo` (se benefician automáticamente del mismo método). Verificado en navegador con un relevo real (login → confirmar turno → logout → login de nuevo, mismo día → el modal volvió a salir). 4 tests nuevos (login real vía `POST /login`, no `actingAs`), 319/319 en ese momento. |
| 2026-09-09 | **Compras — acceso abierto a moderador** (antes `admin.only`, estrictamente admin). Cambio de negocio explícito y sin condiciones del cliente: middleware cambiado al alias `admin` (ya existente, permite admin+moderador, mismo patrón que Reportes) — paridad total, no acceso parcial: moderador ahora también puede aprobar/anular/editar compras pendientes y editar código de lote. Widget del dashboard actualizado (`userRole !== 'vendedor'`). 5 tests invertidos de "moderador no puede" a "moderador sí puede". 315/315 en ese momento. |
| 2026-09-09 | **Fix: campos de contraseña con toggle "mostrar/ocultar" se escribían en mayúscula.** El componente `Input` compartido pone mayúscula automática para `type="text"` (pedido del cliente, en todo el proyecto) — pero los 5 campos de contraseña con ojito (`Cuentas/Edit.tsx`, `Empleados/{Edit,Create}.tsx`, `Productos/Edit.tsx`, `Productos/Vendor/Index.tsx`) cambian su `type` a `"text"` al revelar la contraseña, activando la regla sin querer. Es solo visual (CSS `text-transform`, no altera el valor real enviado al backend) pero confundía. Fix: `className="normal-case"` en los 5 lugares (mecanismo de escape ya documentado en el propio componente). |
| 2026-09-08 | **Cuentas → grilla de "tarjetas de banco" — implementado y verificado en navegador de punta a punta.** Idea parqueada desde el 2026-08-25 (mockups en `logos_money/`), construida en 3 fases. **Fase 1 (catálogo):** `app/Services/CatalogoTarjetasService.php` — catálogo en código, no en base de datos (`cuentas.imagen` ya existía como string libre desde agosto): 5 bancos "internas" (BANDEC, BPA, Metropolitano, Clásica, Tropical) + 4 "externas" (Visa, Mastercard, y **Zelle/Discovery agregados esta sesión** — procesados con Python/Pillow, fondo removido, recortados al silueta de tarjeta, sin herramientas de generación de imágenes — solo conversión/limpieza de lo que el cliente ya generó con ChatGPT). Validación `in:` contra los slugs en `CuentaController::store()`/`update()`, siempre opcional. **Fase 2 (selector):** `SelectorBancoTarjeta.tsx` — Dialog + Tabs Internas/Externas + grilla de logos clickeables, integrado en Create/Edit. **Fase 3 (grilla):** `Cuentas/Index.tsx` pasa de tabla a grilla de `CuentaCard.tsx` (header con la tarjeta real o ícono genérico, número enmascarado decorativo determinístico por id, saldo, 3 badges — tipo_titular/banco/moneda), filtro nuevo por Banco (Internas/Externas + "Sin banco asignado"), menú "···" con Ver/Editar/Eliminar. **De paso, mismo criterio aplicado también a `Show.tsx`** (no estaba en el plan original, lo pidió el cliente al ver la Fase 3 terminada): mismo header reactivo, más una fila "Banco" en la card de información. **Toque final:** el header de las 3 páginas (Create/Edit/Show) usa efecto **bleed** (`docs/patron-mascota-bleed.md` Variante A — sin `overflow-hidden`, anclado `bottom-0`, la tarjeta se sale del borde superior) en vez de quedar contenida adentro — primer intento quedó contenido y chico, corregido a pedido explícito del cliente. En Create/Edit el header reacciona **en vivo** al selector, antes de guardar. 8 tests nuevos (`CuentaTest.php`), suite completa **314/314 verdes**, Pint y eslint limpios. Verificado en navegador con una cuenta real ya existente (`WICHO CUP 1`, BPA) más una cuenta de prueba creada y borrada al final. **Pendiente, pausado por el cliente:** qué dato ocupa el primer badge de las cards (hoy `tipo_titular` como marcador de posición). Detalle completo en memoria `project_cuentas_rediseno_tarjetas_bancarias`. |
| 2026-09-08 | **"Atendido por" / Turnos — implementado y verificado en navegador de punta a punta.** Diseño ya resuelto en conversación (2026-09-05/07, ver memoria `project_atendido_por_idea`), construido en una sesión: (1) **Backend** — tabla `turnos_vendedor` (log append-only: `user_id`, `nombre_vendedor`, `iniciado_en`; "turno activo" = fila más reciente por usuario), `ventas.turno_vendedor_id` (FK nullable), `TurnoVendedorController::store()` + ruta `turno-vendedor.store`, middleware `RequireTurnoActivo` (alias `requiere.turno`, aplicado al grupo principal de rutas) que bloquea POST/PUT/PATCH/DELETE para `moderador`/`vendedor` sin turno de hoy — admin exento, GET nunca se bloquea, `logout` y la propia ruta de captura exceptuadas explícitamente. Prop compartida Inertia `turno` (`requiereCaptura`/`nombreVendedor`/`sugerido`/`historial`) en `HandleInertiaRequests`, mismo mecanismo que `tasas`/`TasasFlotante`. `VentaController::procesarVenta()` guarda `turno_vendedor_id` al crear la venta. (2) **Frontend** — `TurnoBloqueante.tsx` (AlertDialog global montado en `app-sidebar-layout.tsx`, sin cancelar/escape/clic-afuera, `open` atado directo a la prop del servidor) y `TurnoIndicador.tsx` (badge persistente en el header con Dialog voluntario para cambiar de persona a mitad de turno, historial como chips). (3) **Integración** — Rastreo de Operaciones (`DetalleOperacionService::detalleVenta()` + `detalle-operacion.tsx`) y el Ticket de impresión (`VentaController::imprimir()` + `Vendor/Imprimir.tsx`, en **mayúscula** a pedido explícito del cliente, "para prevenir" inconsistencias de escritura) ambos muestran "Atendido por" junto al campo `usuario`/`Vendedor` existente (la cuenta de punto de venta), no en su lugar. **Regresión real encontrada y arreglada en el camino:** el middleware nuevo rompió 37 tests existentes (`VentaTest`, `CierreCajaTest`, `TransaccionFinancieraTest`, `MovimientoTest`, `MovimientosProrrateoTest`, `CuentaTest`, `ProveedorTest`) porque esos tests hacían escrituras como vendedor/moderador sin turno capturado — arreglado con un helper nuevo `crearTurnoActivo()` en `tests/Pest.php`, aplicado en los 7 archivos. 2 tests nuevos para el detalle en Rastreo, 2 para el Ticket, 11 para el feature en sí. Suite completa: **303/303 verdes**. **Verificado en navegador real** (no solo tests): usuario/turno QA temporal creado por tinker, venta real de $38 procesada como vendedor con turno activo, confirmado `turno_vendedor_id` en BD, "Atendido por: MARIA LÓPEZ" en el Ticket y "Atendido por: María López" en Rastreo de Operaciones — venta anulada y todo el rastro de prueba (usuario, turno, venta) borrado al final, stock verificado que volvió a su cantidad original. **Aviso para el despliegue a producción** (cuando corra el `migrate:fresh --seed` que el cliente ya tiene planeado): en cuanto cualquier `moderador`/`vendedor` real entre por primera vez a la base nueva, le va a salir el diálogo bloqueante — comportamiento esperado, no un bug, vale la pena avisarlo de antemano. **Extendido más tarde el mismo día a Gastos/Ingresos/Transferencias** (`turno_vendedor_id` agregado en `GastoController`, `IngresoController`, `TransferenciaController`, mismo campo/lógica que Venta) — **Compras queda excluida a propósito**, decisión explícita del cliente ("quien compra es el admin"). |
| 2026-09-08 | **Reconciliación de este documento contra el código real.** Este archivo llevaba desde el 2026-08-25 sin actualizar su lista de prioridades (aunque el historial de abajo sí se movió hasta el 09-05) y arrastraba varios ítems ya resueltos. Se verificó cada ítem de Alta/Media/Baja contra el código (grep/Read directo, no solo memoria): removidos los ya resueltos (Combobox del POS, permisos de `VentaController`, F1, tests de `ajustarSaldo`, páginas duplicadas en minúscula, rutas `ventas/validar-stock`/`actualizar-tasas`), reverificados los que siguen abiertos (modal de confirmación de `Cierres/Create.tsx`, 4/8 cards sin degradado en `Vendor/Show.tsx`, rutas huérfanas de `movimientos.aprobar` y `vendedor.php`, Combobox faltante solo en `Almacenes/Show.tsx`/`Productos/Edit.tsx`), y agregados 2 hallazgos nuevos: `Reportes/Report/ProductosMasVendidos.tsx` es un archivo de 0 líneas (rompe al abrir el reporte) y `app/Services/DetalleOperacionService.php`/`resources/js/components/detalle-operacion.tsx` existen y están en uso real pero no estaban documentados en ningún lado — pregunta pendiente al cliente sobre ese trabajo. También se agregó como primer ítem de Alta prioridad la feature "Atendido por / Turnos", cuyo diseño quedó 100% resuelto en conversación el 2026-09-07 con instrucción explícita del cliente de arrancarla "mañana" (hoy). Sin cambios de código en esta sesión, solo documentación. |
| 2026-09-07 | **Compras — máquina de estados (pendiente/aprobada/anulada), edición y anulación con auditoría, trazabilidad por lote.** Motivado por una pregunta real del cliente ("¿qué pasa si hay que anular una compra?") — hasta entonces `Compra` no tenía estado ni forma de editarse/anularse. Diseño: `store()` mueve el dinero de inmediato pero deja el stock diferido; `aprobar()` es el único momento que suma stock real y genera `LoteStock`; una vez aprobada, la compra queda inmutable. Anulación (solo desde `pendiente`) en 2 variantes — reversión total, o conversión a fondo del proveedor/cliente-fuente (regla dura: la porción de deuda nunca se convierte en fondo, siempre vuelve a 0). Edición (solo desde `pendiente`) trata la compra completa como nueva: revierte todos los efectos monetarios y reprocesa desde cero, con nota obligatoria auditada en `compra_ediciones`. Trazabilidad por lote: un `LoteStock` por línea de `compra_producto`, código autogenerado editable (`LOTE-{compra_id}-{línea}`). 34 tests nuevos, 288/288 verdes, verificado en navegador con datos reales. **Gaps conocidos, no resueltos**: sin vista de consulta de lotes en ningún frontend; `DistribucionCostosController` no sincroniza `lotes_stock.precio_costo` al repartir costos adicionales (riesgo de desincronización). Detalle completo en memoria `project_compras_estado_anulacion_lotes` — no hay doc dedicado en `docs/arreglos-pendientes/` todavía. |
| 2026-09-07 | **Cuentas — 7 tests nuevos cubriendo `ajustarSaldo`** (contraseña admin real vía `Hash::check`, motivo obligatorio, bloqueo a moderador/vendedor) y clasificación `con_fondo`/`en_deuda`/`neutro` en `index()`. 269/269 tests verdes. Hallazgo real: código muerto en `CuentaController::update()` — el chequeo interno de rol para el cambio de saldo nunca se alcanza para moderador/vendedor porque el middleware `check.cuenta.permission` ya bloquea antes. Mismo día: reconciliación completa de la lista de prioridades de este documento contra el código real (ver entrada 2026-09-08 arriba) y detección de trabajo sin documentar (`DetalleOperacionService`/`detalle-operacion.tsx`, ver Media prioridad). |
| 2026-09-05 | **Impresión de venta por duplicado (cliente + punto de venta) + fix de `exportToPDF` en Rastreo de Operaciones.** Pedido del cliente: al imprimir una venta debe salir doble, una copia para el cliente y otra que se queda en el punto de venta como comprobante/garantía. `Vendor/Imprimir.tsx` ya repartía Ticket(interno)/Factura+Garantía(cliente) pero la mitad inferior de la hoja A4 quedaba en blanco y el Ticket no llevaba garantía ni firma — se extrajo el contenido a variables reutilizables y se duplicó en ambas mitades de la hoja (ancladas por offset fijo en mm, no por alto renderizado, para no reintroducir el bug de Chrome ya documentado). Verificado en navegador contra la venta #400 y **confirmado por el cliente en PDF real**. De paso, corregido un hallazgo arrastrado desde agosto: el botón "Exportar PDF" de Rastreo de Operaciones generaba un PDF con 9 columnas viejas (incluía Fecha/Usuario) desincronizadas de las 7 columnas reales en pantalla — ahora coinciden exactamente. **Prioridad del módulo Reportes bajada explícitamente** por el cliente (los 14 reportes restantes se trabajan solo bajo petición, no proactivamente). Detalle completo en `docs/arreglos-pendientes/resumen-cambios-2026-09-05.md` |
| 2026-09-01 | **Saldo anterior/posterior en Venta y Compra (4 fases) + limpieza de `DatabaseSeeder.php` para producción.** Gasto/Ingreso/Transferencia ya mostraban el saldo antes/después de cada cuenta tocada (en `movimientos_financieros`) pero solo en Rastreo de Operaciones; Venta y Compra —las operaciones de mayor volumen— no capturaban nada en absoluto, y ni siquiera lo que sí existía se mostraba en las páginas propias de Cuentas/Clientes/Proveedores. **Fase 1-2:** nuevas columnas `saldo_anterior`/`saldo_posterior` en `pago_ventas`/`ventas` (comisión PV/gestor/mensajero) y `compra_pago`/`compras` (receptor), capturadas en `VentaController::aprobarVenta()` y `CompraController::store()`. **Fase 3:** Rastreo de Operaciones muestra ese saldo para Venta/Compra reutilizando el componente `EntidadMovimientoCard` que ya usaban Gasto/Ingreso/Transferencia (cero componentes nuevos), más una tarjeta nueva de motivo de anulación cuando una venta queda cancelada (idea del cliente sobre la marcha). **Fase 4:** el mismo dato ahora se ve también en el historial propio de Cuentas/Clientes/Proveedores (`Show.tsx` de cada uno) — línea compacta bajo el monto, sin columnas nuevas. Verificado end-to-end en navegador con una Venta y una Compra reales creadas con datos de prueba aislados (`QA-TEST *`), aprobadas, y la venta anulada vía el endpoint real — todo el rastro de prueba se borró al final, dashboard verificado que vuelve exacto al Capital Financiero original. **De paso, preparando el `migrate:fresh --seed` de mañana:** `DatabaseSeeder.php` sincronizado con los 11 usuarios/24 almacenes/15 categorías reales de producción (varios nombres habían quedado desactualizados), clientes de ejemplo quitados, cuentas base sin cambios. **Bug real crítico encontrado en el camino:** el seeder tenía `'deuda' => 0` al crear cuentas, columna eliminada hace tiempo — invisible en uso normal porque Eloquent la descarta en silencio, pero `php artisan db:seed` desactiva esa protección globalmente (`Model::unguarded()` en `SeedCommand::handle()`), así que el seeder se hubiera caído a mitad de camino mañana. Corregido y cubierto con `tests/Feature/DatabaseSeederTest.php` nuevo (SQLite en memoria, nunca tocó la base real). 258/258 tests verdes. Detalle completo en `docs/arreglos-pendientes/resumen-cambios-2026-09-01.md` |
| 2026-08-25 | **Cuentas — limpieza de `tipo` + preparación para el rediseño de tarjetas de banco (idea de media prioridad, empezar pronto).** El cliente pidió quitar `otro` del selector "Tipo de Activo" (0 cuentas reales lo usaban) — migración recorta el enum de la columna a `tarjeta`/`efectivo`, validación del controlador y los 3 archivos frontend (`Create`/`Edit`/`Index`) actualizados, `CuentaFactory` corregida para no generarlo más. De paso, agregada `cuentas.imagen` (string nullable, sin consumidor todavía) para la idea de mostrar cada cuenta como una tarjeta con el logo real del banco (BANDEC/BPA/Metropolitano/Visa/Mastercard, imágenes ya importadas en `public/projects/`) — 3 decisiones de diseño confirmadas, el campo que falta (qué banco tiene cada cuenta) queda para cuando se retome. Detalle completo en memoria `project_cuentas_rediseno_tarjetas_bancarias`. 195/195 tests verdes.
| 2026-08-25 | **Dashboard — "Totales" de Tabla 2 corregido y ahora coincide exacto con Capital Financiero de `/logistica`.** Dos hallazgos en el mismo hilo, mismo día que la fila Clientes de abajo. **(Fase 6)** Bug real: `actualizarComparacionMensual()` convertía el monto combinado de CUP (este sistema tiene 2 monedas reales codificadas "CUP", efectivo tasa 675/tarjeta tasa 685) a USD usando la tasa de una sola fila `monedas` arbitraria, inflando el total — Totales daba más que Capital Financiero aun sin Proveedores integrado. Corregido derivando una "tasa efectiva" (`original ÷ equivalente`, ambos ya sumados correctamente por-cuenta) en vez de confiar en una tasa cruda. El cliente pidió después un desglose "collapsible" para CUP; se construyó el soporte de backend (`detalle` por `moneda_id`) pero, al verificar la aritmética a mano, confirmó que el número ya combinado es correcto y decidió no construir esa UI — el campo queda disponible sin consumidor. **(Fase 7)** El cliente insistió en por qué Totales seguía sin coincidir con Capital Financiero — la diferencia real era el Balance Neto de Proveedores (-149.670,24), excluido a propósito desde antes. Se agregó fila sintética `PROVEEDORES` a Tabla 2 (mismo mecanismo que Clientes/Inventario), Totales ahora = Capital Financiero exacto. **Bug real encontrado en el camino:** `moneda_codigo` era `VARCHAR(10)`, "PROVEEDORES" (11 caracteres) rompía el insert en MySQL — invisible en el test suite porque corre sobre SQLite (sin límite de longitud). Migración nueva amplía la columna a `VARCHAR(20)`. Detalle completo en `docs/arreglos-pendientes/dashboard-resumen-financiero-2026-08-12.md` (Fases 6-7). Suite 195/195 verde, verificado en navegador.
| 2026-08-25 | **Dashboard — Tabla 2 gana fila "Clientes", Clientes+Inventario ahora suman a "Totales".** Hueco real encontrado analizando `dashboard.tsx`: Tabla 1 (Resumen Financiero) ya sumaba clientes+proveedores+inventario en la moneda principal, Tabla 2 (Comparación Mensual) no trackeaba clientes/proveedores en absoluto. Se agregó fila sintética `CLIENTES` (mismo mecanismo que `INVENTARIO`, Fase 4a) — Proveedores queda fuera a propósito, el cliente lo pidió explícitamente. Clientes e Inventario ahora suman al total de "Totales" (antes Inventario quedaba fuera a propósito); reordenado monedas→Inventario→Clientes→Totales. Avisos azules reubicados/agregados en ambas tablas para explicar por qué la moneda principal no cuadra con solo sumar cuentas. **2 bugs reales corregidos de paso** (no relacionados): ícono `TrendingUp` en 2 tarjetas de "pérdidas" (debía ser `TrendingDown`), chequeo de rol redundante en Comparación Mensual. Verificado que `dashboard.financial.states` SÍ está bien protegido por rol server-side (falsa alarma). Detalle completo en `docs/arreglos-pendientes/dashboard-resumen-financiero-2026-08-12.md` (Fase 5). Suite 195/195 verde.
| 2026-08-25 | **Prorrateo de Costos en Movimientos, implementado y probado (195/195 tests verdes).** Un movimiento hacia un almacén ajeno (vendedor: condicional a lo que tiene asignado; admin/moderador: siempre, corregido dos veces en la sesión hasta llegar a esta regla) puede prorratearse desde "Distribución de Costos" — acción libre, sin gate, `recibir()` sin cambios. Nueva pestaña "Movimientos" en `/distribucion-costos` (lote por checkboxes, distribuir u omitir en bloque), reusa el motor de cálculo de Fase 3. Esquema: 4 columnas nuevas en `movimientos`, tabla `cost_distribution_movimientos`, `cost_distributions.purchase_id`/`account_id` ahora nullable (recreación de tabla, datos reales preservados y verificados). Comando `movimientos:backfill-requiere-prorrateo` corrido en real: 52/52 movimientos existentes quedaron clasificados. 3 bugs reales encontrados y corregidos en el camino: validación `prohibited_with` no existe en esta versión de Laravel, botón "Limpiar filtro" de la pestaña Movimientos borraba las claves de query equivocadas, y la pestaña activa se perdía al paginar (Inertia remonta el componente en cada visita — se deriva del URL ahora). Detalle completo en `docs/arreglos-pendientes/prorrateo-costos-movimientos-2026-08-25.md`. |
| 2026-08-22 | **Sesión larga, 4 frentes.** (1) **Movimientos — editar antes de enviar**: nuevo `MovimientosController::actualizar()` (solo en `pendiente_confirmacion`, revalida stock, sincroniza `MovimientoDetalle`), botón lápiz + diálogo en `Movimientos/Index.tsx`, 4 tests nuevos. **Bug real encontrado probando la feature**: `recibir()` marcaba "Recibido Completo" comparando solo la suma total, no cada línea — un movimiento con una línea de menos y otra de más que se cancelaban en el total (caso real: movimiento #52) quedaba mal clasificado. Corregido a comparación por línea; nuevo comando `php artisan movimientos:corregir-recibido-completo --dry-run` para corregir datos históricos en producción (local: solo #52 de 47 estaba afectado, ya corregido a mano). Columna "Diferencia" agregada al diálogo "Ver Productos" de `Index.tsx` (ya existía en `Show.tsx`). `getProductosPorAlmacen()` ahora filtra `stock_disponible >= 1`. (2) **Proveedores/Show.tsx, Proveedores/index.tsx, Clientes/Index.tsx — pasada de consistencia visual**: headers degradados nuevos, badges de estado unificados a rojo/ámbar/esmeralda (antes usaban `variant` genérico de shadcn o gris), modal casero de detalle de compra en `Proveedores/Show.tsx` reemplazado por filas colapsables (mismo patrón que `RastreoOperaciones.tsx`). **Bug real de tipos**: `Proveedor::saldo_proveedor`/`Cliente::deuda_pago_cliente` usan cast `decimal:2`, que Laravel serializa como *string* en JSON — comparaciones `=== 0` en el frontend nunca coincidían para un saldo exacto en $0.00 (clasificaba mal como "Fondo" en vez de "Neutro", y el filtro "Neutro" no encontraba esos casos). Corregido con `Number(...)` antes de comparar en ambas vistas. (3) **Riel de accesos rápidos → "Dynamic Island"**: nuevo `resources/js/components/quick-access-rail.tsx`, riel fijo en el borde derecho (Cierres de Caja/Mis Ventas/Notificaciones, filtrado por rol), montado globalmente en `app-sidebar-layout.tsx`. Mismo día, rediseñado a pedido del cliente como cápsula que expande junta al hover (no tooltips por ítem) con contador de notificaciones sin leer en vivo (poll cada 30s). (4) **Placeholder en campos numéricos**: 3 archivos corregidos (`Monedas/Create.tsx`, `Movimientos/Index.tsx`, `Proveedores/Create.tsx`) para que arranquen vacíos con placeholder en vez de mostrar un `0` real — `Monedas/Edit.tsx` dejado sin tocar a propósito porque ahí el 0 puede ser un dato real guardado, no un campo sin llenar. **Pendiente parcial, solo analizado, no implementado**: `docs/modelo-costo-producto/` — módulo completo de "Formación de Costos y Precios", ver fila de "Media prioridad" arriba y memoria `project_costo_promedio_ponderado`. |
| 2026-08-20 | **Módulo nuevo "Distribución de Costos"**, reemplaza el prorrateo enterrado en `TransaccionController.php` (sin sidebar, solo alcanzable desde un botón dentro de una compra). Nuevo `DistribucionCostosController` + rutas propias + tablas pivote (`cost_distribution_cuentas`, `cost_distribution_compras`) + `cost_distributions.user_id`. La fórmula manual vieja tenía un bug real (sumaba el monto directo al costo unitario sin dividir entre unidades); la nueva es 100% automática por peso proporcional (`costo_actual×cantidad / total_compra`), verificada con el cliente usando datos reales de las compras #10/#11 y confirmada en navegador. Financia con una o varias cuentas CUP/USD mezcladas, cubre una o varias compras a la vez ("lote"). Nuevo historial (`/distribucion-costos/historial`) y detalle por distribución (`/distribucion-costos/{id}`) con desglose por producto e historial de costo completo. 2 bugs reales encontrados y corregidos en el camino (botón "Detalles" apuntaba al formulario en vez de al historial; `Undefined variable $distribucion` en un closure). 172/172 tests verdes, sin commitear. Detalle completo en `docs/arreglos-pendientes/distribucion-costos-modulo-2026-08-20.md` |
| 2026-08-15 | **Dos hallazgos de la sesión del 2026-08-14 anotados como "media prioridad" a pedido del cliente** (no se tocó código): (1) `VentaController.php` sin control de permisos/dueño en las acciones que mueven dinero (`aprobarVenta`/`anularVenta`/etc.) — cualquier vendedor puede hoy aprobar/anular la venta de otro; más bugs relacionados (campos mensajero muertos, "mensajero propio" a medias, modal "Crear Cliente" sin mostrar errores de campo). (2) `Vendor/Show.tsx`: quedan 4 de 8 cards sin header degradado (Destinatario, Comisión Punto de Venta, Gestor-Comisión, una cuarta sin confirmar). Ver detalle en la sección "Media prioridad" arriba. También corregido: el checklist de migración sileo (`docs/arreglos-pendientes/migracion-toasts-sileo-2026-08-14.md`) tenía `Movimientos/Index.tsx` marcado `[ ] Pendiente` cuando ya está migrado (verificado en código) — actualizado a `[x]` |
| 2026-08-13 | **Precios de Venta — modal masivo por almacén (`/disponibles`), admin-only.** Nuevo botón "Buscar Producto" abre un modal de dos paneles: buscar producto (autocompletado client-side, sin endpoint nuevo, sobre los datos ya cargados), ver en qué almacenes está disponible con su precio actual, marcar varios con checkbox, aplicar el mismo precio/comisión a todos a la vez. Backend: `PUT /disponibles/bulk-actualizar` (`ProductoVendedorController::updateBulk()`, ruta bajo middleware `admin.only`), transacción única, `PrecioHistorial` solo donde el precio realmente cambió. **Exige reconfirmar contraseña** antes de aplicar (`Hash::check`, mismo patrón que el cambio de costo en `ProductoController::update()`, pero siempre, no condicional). Reordenado el resto de la vista: estadísticas arriba, controles del almacén (selector con autocompletado + buscar + exportar/importar) agrupados, subtítulo Marca/Modelo/Capacidad visible en la tabla sin hover, y los widgets "Con Precio"/"Sin Precio" ahora funcionan como filtro-toggle de la tabla. **2 bugs reales encontrados y corregidos de paso**: (1) las imágenes de producto usaban `/storage/{imagen_producto}` (403) en vez de `/{imagen_producto}` — este proyecto sirve imágenes desde `public/productos/` vía `asset()`, no desde el disco `storage`; corregido también en el tooltip preexistente de la tabla. (2) el widget de precio de costo en el modal usaba `text-sidebar` (color rojizo del tema, sin contraste en dark mode) en vez de un color neutro. **Hallazgo sin resolver, bloquea tests**: `producto_vendedors` tiene desfase de esquema MySQL/SQLite — la migración `2026_05_30_000001_refactor_producto_vendedors_unico_por_almacen.php` nunca corrió en SQLite (`if (driver !== 'mysql') return;`), así que los 9 tests ya escritos en `tests/Feature/ProductoVendedorTest.php` no pueden correr hasta agregar una migración nueva compatible con SQLite. Detalle completo en `docs/arreglos-pendientes/precios-venta-modal-busqueda-producto-2026-08-13.md` |
| 2026-08-10 | **Módulo Compras — perfeccionamiento del formulario de alta y 3 bugs de UX resueltos.** Nueva sesión, foco en `Comprar/Index.tsx`/`CompraController.php`, todo verificado en navegador contra datos reales. **(1) Autocompletado de productos existentes**: nuevo endpoint `compras.productos.buscar` (reutiliza `Producto::scopeBuscar` y el accessor `cantidad_total`), dispara desde Nombre/Marca/Modelo, autocompleta Marca/Modelo/Capacidad/Categoría (nunca Color ni Precio, a propósito) y avisa stock/costo actual desglosado por almacén antes de sobreescribir el costo. **(2) Formulario reorganizado** en 3 grupos visuales (`FieldSet`: Identificación / Destino y Categoría / Datos Comerciales), prefijo `$` en Precio, Enter para agregar sin mouse, preview en vivo del código autogenerado. **(3) Tabla del carrito rediseñada**: envuelta en `Card` (antes `min-h-[100vh]` dejaba un vacío enorme con pocos productos), zebra striping, footer con tokens del tema en vez de grises hardcodeados que rompían el modo oscuro. **(4) Migración `compra_producto`**: pasó de clave primaria compuesta `(compra_id, producto_id)` a `id` autoincremental — el mismo producto ya puede tener varias líneas en una compra (distinto almacén/color) sin fusionarse ni promediar precio; ajustados `TransaccionController` (distribución de costos/transportación) y los reportes que contaban "veces comprado" para no asumir una fila por producto. 1 test nuevo en `CompraTest.php` (suite completa: 140/140 verdes). **(5) Tres bugs de UX arreglados**: Combobox roto por mouse dentro del diálogo "Editar Producto" (mismo patrón ya documentado en `pendiente-combobox-reemplazo.md`, resuelto con `container` + ref-callback porque el diálogo vive en un `.map()`); dos listas de clientes desincronizadas entre el combobox superior y el paso de pago (helper `sincronizarClienteEnListas` compartido); errores de validación del backend que solo se mostraban si fallaba el producto en índice 0 del carrito, ahora identifican el producto real sin importar el índice. Nuevo patrón documentado en `docs/patron-dialog-formulario-grande.md` (diálogo grande de dos paneles, más las 2 variantes de una sola columna ya existentes en el proyecto, y un anti-patrón identificado en `Proveedores/Show.tsx` que no usa el componente `Dialog` real). Pendiente, explícitamente pausado por el cliente: reemplazar el paso de pago "Pagar Ahora" por el patrón `PaymentForm`/`PaymentList` que ya usa Ventas — el más grande de la lista, no arrancar sin decisión explícita |
| 2026-08-08 | **Verificación de estado + actualización de documentación**: confirmado que Fases 7/8/9 de Rastreo de Operaciones (abajo) ya están commiteadas (`ee8942c4`..`a5f2e052`), árbol de trabajo limpio. Suite de tests re-corrida: **139 tests, 138 passed, 1 failed** — la regresión de `public/build/manifest.json` (fila siguiente) **no está activa hoy** (esa carpeta no existe en disco); el único fallo es nuevo y distinto: `RastreoOperacionesTest.php:115`, `UniqueConstraintViolationException` en `monedas.nombre_moneda` — parece colisión aleatoria del Faker generando un nombre de moneda duplicado en el factory, no un bug de la feature. Sin arreglar, solo reportado. `context.md` e `INDEX.md` actualizados en la misma pasada para reflejar Fases 7-9 |
| 2026-08-06 | **Rastreo de Operaciones — filtros nuevos: Cliente/Proveedor/Cuenta multiselect + buscar por número de referencia (Fase 9, cerrada)**: pedido explícito del cliente ("ni yo mismo sé qué tipo es esa operación") — escribir un número como `34` ahora encuentra `Venta #34`/`Gasto #34`/etc. sin saber el tipo (`ctype_digit($buscar)` agrega un match exacto por id, sumado a la búsqueda de texto libre de siempre). Los 3 filtros `<select>` de una sola cuenta se reemplazan por combobox multiselect con chips (Cliente/Proveedor/Cuenta) — primer uso real en el proyecto del componente `resources/js/components/ui/combobox.tsx` (`@base-ui/react`), que ya existía sin usarse. Backend: cada filtro aplica solo donde el dato tiene sentido por tipo (ej. Proveedor nunca calza en Venta — el subquery de Venta se fuerza a 0 resultados si el filtro está activo), usando `whereExists` contra `pago_ventas`/`compra_pago` para no duplicar filas cuando una operación tiene varios pagos. `clientes`/`proveedores`/`cuentas` se mandan como props a todos los roles (no es dato de costo). Verificado con tinker y en navegador (selección, chips, remover, persistencia al recargar), y 5 tests nuevos en `RastreoOperacionesTest.php` (135 tests, todos verdes). Detalle completo en `docs/arreglos-pendientes/rastreo-operaciones-rediseno-2026-08-01.md` (Fase 9) |
| 2026-08-06 | **Rastreo de Operaciones — Compras reintegradas al reporte (Fase 7, cerrada)**: quinto tipo junto a Venta/Gasto/Ingreso/Transferencia, admin/moderador-only (vendedor no ve la clave `Compra` en los widgets ni puede forzarla por `?tipo=Compra`, es dato de costo y no había forma de acotar "solo lo mío"). Requirió migración `add_user_id_to_compras_table` (nullable) porque `compras` nunca guardó quién la hizo; `CompraController::store()` lo captura de ahora en adelante, histórico queda en `—`. Backend: `RastreoOperacionesController` gana `construirSubqueryCompra()`/`transformarCompra()` con su propia forma de detalle (proveedor/cliente que recibió el pago, uno o varios métodos de pago que salieron del sistema, productos comprados sin costo/ganancia). Frontend: `RastreoOperaciones.tsx` gana el tipo `'Compra'` en badges/widgets/filtro/columnas y un nuevo componente de detalle expandido. Verificado con tinker + navegador contra las 5 compras reales existentes, y con 4 tests nuevos en `RastreoOperacionesTest.php` (130 tests, todos verdes). Detalle completo en `docs/arreglos-pendientes/rastreo-operaciones-rediseno-2026-08-01.md` (Fase 7) |
| 2026-08-06 | **Rastreo de Operaciones — Fecha/Hora fuera de la tabla y pagos de Venta con badges alineados por color**: dos ajustes emergentes sobre la Fase 8, mismo día. (1) Columna `Fecha/Hora` retirada de la tabla principal a pedido del cliente (sigue en el detalle expandido); tabla queda en 7 columnas: `Referencia \| Cuenta Envía \| Monto \| Cuenta que Recibe \| Monto \| Tasa de la Operación \| Detalles`. (2) En Venta, "Cuenta que Recibe"/"Monto"/"Tasa de la Operación" dejaron de fusionar cuenta+monto en un badge de texto — ahora cada columna muestra un `Badge` por pago, alineados por índice y coloreados con la misma paleta nueva (`colorPago()`) para identificar de un vistazo qué monto y tasa van con qué cuenta cuando una venta tiene varios pagos. Verificado en navegador contra la venta real `#392` (dos pagos, tasas distintas: 685 y 675). Hallazgo nuevo, sin arreglar: el botón "Exportar PDF" del mismo reporte quedó con las columnas viejas, desincronizado de la tabla en pantalla. Solo frontend, sin cambios en el controller. Detalle en `docs/arreglos-pendientes/rastreo-operaciones-rediseno-2026-08-01.md` (Fase 8) |
| 2026-08-06 | **Rastreo de Operaciones — reorganización de columnas de la tabla (Fase 8, UX)**: `Referencia` absorbe la columna `Tipo` (badge coloreado + chevron en una celda). Tabla pasa a formato "partida doble": `Referencia \| Fecha/Hora \| Cuenta Envía \| Monto \| Cuenta que Recibe \| Monto \| Tasa de la Operación \| Detalles`. En Venta, "Cuenta que Recibe" muestra un badge por cada pago (puede haber varias cuentas en una sola venta). Columna `Usuario` retirada de la tabla — **pendiente: Venta se quedó sin ningún lugar donde mostrar quién la hizo**, decidir dónde reponerla. Solo frontend, sin cambios en el controller. Detalle en `docs/arreglos-pendientes/rastreo-operaciones-rediseno-2026-08-01.md` (Fase 8) |
| 2026-08-06 | **Hallazgo (no arreglado todavía): regresión de la suite de tests** — 23 de 126 tests fallan con 409 "Invalid JSON was returned from the route" en rutas Inertia (`CierreCajaTest`, `CuentaTest`, `RastreoOperacionesTest`). Causa: `public/build/manifest.json` ahora existe en disco (antes no) e `Inertia\Middleware::version()` lo hashea automáticamente; los tests no mandan `X-Inertia-Version`, así que Inertia responde 409 de conflicto antes de que la ruta corra. No es un bug de la app — es un artefacto de tener assets compilados al correr la suite. Pendiente de arreglar como parte de "actualizar los tests" |
| 2026-08-01 | **Cuentas — historial de operaciones en `Cuentas/Show.tsx`**: nuevo, antes solo mostraba datos estáticos. `CuentaController` agrega 3 métodos (`obtenerHistorialTransacciones`/`Ventas`/`Compras`) que arman el historial real combinando 4 fuentes que tocan `saldo_cuenta` (`movimientos_financieros`, `pago_ventas`, `ventas` directo para comisión PV/gestor/mensajería, `compra_pago` — las últimas 3 NO quedan logueadas en `movimientos_financieros`, ver B9 para el bug de bindings encontrado al agregar filtros). Frontend: 3 Cards separadas (Compras/Ventas/Transacciones, estilo `Proveedores/Show.tsx` y `Clientes/Show.tsx`) con badge de conteo, botón "Detalle" que enlaza al registro real (`ventas.show`/`transacciones.show`/`comprar.show`), buscador + filtro por tipo + rango de fechas por Card (paginación real, `pagina_transacciones`/`pagina_ventas`/`pagina_compras` como nombres de página independientes). Compras se oculta a `vendedor` (mismo criterio que `precio_compra`). 10 tests nuevos en `tests/Feature/CuentaTest.php` |
| 2026-08-01 | **Cuentas — acceso de `vendedor` a `Cuentas/Show`**: antes solo `admin` (middleware `check.cuenta.permission` binario). Ahora `admin`/`moderador` siempre, `vendedor` solo si la cuenta está en `auth()->user()->cuentas()` (mismo patrón que Gasto/Ingreso/Transferencia). La ruta `show` salió del middleware compartido; el chequeo vive en `CuentaController::show()`. `Cuentas/Index.tsx` muestra "Ver detalles" también a vendedor (antes oculto); "Editar"/"Eliminar" siguen admin-only |
| 2026-08-01 | **Cierres — fix de transferencias entre monedas distintas**: `CierreCajaController::obtenerResumenTransferencias()` deduplicaba por `movimiento_id` con un `$seenMovimientos` declarado DENTRO del loop por moneda — para una transferencia que cruza de moneda (ej. CUP→USD) el "lado saliente" y el "lado entrante" caen en buckets de moneda distintos, así que nunca se deduplicaban entre sí y la transferencia salía duplicada en el historial. Fix: el mapa de vistos se movió fuera del loop (alcance global a la función). Frontend (`Cierres/Show.tsx`/`Create.tsx`): al filtrar por una moneda específica, la fila ahora se muestra desde la perspectiva de esa moneda (signo correcto) en vez del `tipo` canónico fijo |
| 2026-08-01 | **Widget "Resumen Financiero del Turno" oculto para `vendedor`** en `Cierres/Show.tsx`/`Create.tsx` — mostraba ganancia de la agencia, dato que no le corresponde ver a ese rol |
| 2026-07-31 | **Tests Pest** — 100 tests nuevos: `VentaTest.php` (24), `CierreCajaTest.php` (13), `MovimientoTest.php` (15), `TransaccionFinancieraTest.php` (17). En el proceso se encontraron y corrigieron 3 bugs reales (B6, B7, B8, ver arriba) y se arreglaron factories desactualizadas (`CuentaFactory`, referenciaba columna `deuda` eliminada) y faltantes (`MovimientoFinancieroFactory`, nueva) |
| 2026-07-31 | **Frontend — Combobox en `PaymentForm.tsx`** (campo "Destino del Pago" en el POS): convertido de `<Select>` a `<Combobox>` con lista combinada cuentas+clientes físicos, búsqueda por texto, truncado con tooltip. Encontrado y arreglado un bug de conflicto Radix `AlertDialog` vs `@base-ui/react` Combobox (portal fuera del focus-trap rompía click y scroll con mouse) — fix genérico en `combobox.tsx` (`ComboboxContent` acepta `container`), documentado en `pendiente-combobox-reemplazo.md` |
| 2026-07-31 | **Frontend — Inputs numéricos sin scroll-to-change**: fix centralizado en `resources/js/components/ui/input.tsx` (`onWheel` hace `blur()` cuando `type="number"`), cubre automáticamente todos los `<Input type="number">` del proyecto |
| 2026-07-30 | **Cierres — desglose de productos y filtros** (8 commits, 14:18→17:02, mal etiquetados "transacciones"): `CierreCajaController` agrega `productos` (nombre/marca/modelo/cantidad) a cada línea de comisión PV y Gestor; campo `moneda` en movimientos financieros del detalle de cierre; deduplicación de transferencias por `movimiento_id`; `Cierres/Show.tsx`/`Create.tsx` separan gastos/ingresos/transferencias `es_propio` vs externos y agregan búsqueda/filtro para cuentas y clientes en la comparativa (~800 líneas modificadas en total) |
| 2026-07-30 | "Compras con 0.90" en `CompraController` (commit `e7fc149e`, previo a los 8 commits de Cierres) |
| 2026-07-28 | **Transacciones 75%** — Gastos, Ingresos, Transferencias funcionales. Arreglos en flujo contable y control de saldos |
| 2026-07-28 | **Cuentas — `tipo_titular`** (externa/personal), eliminación campo `deuda`, unificación `temporales→permanentes` vía migración |
| 2026-07-28 | **Cuentas — soporte saldos negativos** como deudas, ajustes en backend y UI |
| 2026-07-28 | **Arreglo Empleados** — Correcciones en gestión de empleados |
| 2026-07-27 | **Logística II** — Expansión con Create/Edit/Show, layouts de charts (ComprasVentas, ProductosPorAlmacen, KPIsPeriodo) |
| 2026-07-27 | **Arreglo Movimientos** — Correcciones en flujo de movimientos de stock |
| 2026-07-26 | **Logística I** — 4 widgets Capital Financiero (Capital, USD, CUP, EUR) con datos reales, resúmenes de Cuentas/Clientes/Proveedores/Productos. Iconos (`Landmark`, `DollarSign`, `Wallet`, `Euro`), `border-l-4`, `shadow-sm hover:shadow-md`. Limpiados imports no usados y prop `balances`. |
| 2026-07-26 | **Cuentas Index — Row 3 ahora filtra solo permanentes**: nuevo `por_moneda_perm` en backend. Formato moneda con `": "` en toda la vista (ej. `$: 305.834,63`). |
| 2026-07-26 | **Cuentas Index rediseñado** — 3 filas de widgets interactivos clickeables. Filtros combinados + resumen de saldos por moneda. Deudas detectadas desde `saldo_cuenta < 0`. |
| 2026-07-25 | **F6** — Herramienta detección/fusión de duplicados: 2 modales, normalización unicode capacidad, suma cantidades + promedio precios |
| 2026-07-24 | Columnas **Marca, Modelo, Capacidad, Color** agregadas al Excel de export/import de precios (`PreciosVendedorExport`, `PreciosVendedorImport`). `UserController`: filtrado de cuentas para empleados solo `permanentes` |
| 2026-07-15 | `color_producto` añadido a tabla/tooltip en Productos, Movimientos, Compras y todas las vistas donde aparecía el producto |
| 2026-07-15 | `CompraController::show()` implementado — historial de compras recientes en `Comprar/Index`, vista detalle `Comprar/Show`. Fix: parámetro renombrado a `$comprar` para que el route model binding de `{comprar}` funcione correctamente |
| 2026-07-01 | Snapshot de mensajero en cierre_cajas, resumen financiero turno, desglose comisiones PV/Gestor |
| 2026-06-28 | `mensajero_monto_final_cup` editable, TasasFlotante global, validaciones mensajero en aprobar |
| 2026-06-25 | Mensajero multi-moneda en POS (selector moneda + tasa + equivalente USD) |
| 2026-06-23 | Lógica financiera unificada mensajero (propio/externo, USD/CUP) |
| 2026-06-22 | Migraciones mensajero en ventas y almacens |
| 2026-06-02 | Bot Telegram: infraestructura, webhook, notificaciones, comandos |
| 2026-05-30 | Refactor producto_vendedors (único por almacén), comisión en precio_historials |
| 2026-05-29 | Venta especial: campos de estado, flujo admin Telegram |
| 2026-05-25 | Comisión vendedor por item (`comision_unitaria`), tracking `total_comision` |
| 2026-05-21 | Códigos de barras en ventas, fixes de flujo |
